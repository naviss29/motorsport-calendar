"""GUI views — one module per navigation destination.

Each module exposes a single build_*_view() function that returns an ft.Control.
All business logic stays in main_view.py; views are pure layout.
"""
