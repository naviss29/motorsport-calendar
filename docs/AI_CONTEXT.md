# AI_CONTEXT.md

> Fichier de reprise rapide pour une IA. Mis à jour après chaque session.
> Dernière mise à jour : 2026-07-14 (Sprint 59)

---

## État du projet

- **Nom** : motorsport-calendar
- **Version** : 0.2.0 (alpha)
- **Phase** : Sprint 59 — Correction du packaging Flet (le build Linux démarre réellement désormais — `ModuleNotFoundError` du Sprint 58 corrigé et vérifié par un rebuild + lancement réel du binaire, voir `docs/PACKAGING.md` §7) ✅
- **Tests** : 2041 passants (1 skip Windows-only), 0 échouants — couverture ~97 %
- **Branche** : `master`

---

## Stack technique

| Élément | Choix |
|---|---|
| Python | 3.12+ |
| Modèles | Pydantic v2, `frozen=True` |
| HTTP | `httpx.AsyncClient` (async) |
| CLI | Typer + Rich |
| Export ICS | `icalendar` ≥ 5.0 |
| Tests | pytest + pytest-asyncio (`asyncio_mode = "auto"`) |
| Linter | ruff |
| Type checker | mypy |
| Build | hatchling |
| CI | GitHub Actions |

---

## Architecture

```
motorsport_calendar/
├── cache/
│   ├── __init__.py          # export HttpCache
│   └── http_cache.py        # ✅ HttpCache — cache disque JSON, TTL, indépendant httpx
│
├── models/                  # Pydantic frozen — NE PAS MODIFIER sans ADR
│   ├── championship.py      # Championship, ChampionshipCategory
│   ├── circuit.py           # Circuit
│   ├── session.py           # Session, SessionType (StrEnum)
│   └── event.py             # Event, EventStatus (StrEnum)
│
├── providers/
│   ├── base.py              # Provider (ABC) — fetch_events / fetch_championship
│   ├── formula1/
│   │   ├── __init__.py      # ✅ auto-register : registry.register("formula1", _make_provider)
│   │   ├── provider.py      # Formula1Provider — délègue à Formula1Source
│   │   ├── source.py        # Formula1Source (ABC) — get_season(year)
│   │   └── sources/
│   │       ├── jolpica.py   # ✅ IMPLÉMENTÉ — API api.jolpi.ca (Ergast successor, 1950+)
│   │       ├── openf1.py    # ✅ IMPLÉMENTÉ — API openf1.org + HttpCache + JsonDataSource
│   │       ├── ergast.py    # ✅ alias → JolpicaSource (Ergast arrêté fin 2024)
│   │       ├── official.py  # 🔴 STUB — raise NotImplementedError
│   │       └── cached.py    # 🔴 STUB — raise NotImplementedError
│   ├── formula2/
│   │   ├── __init__.py      # ✅ auto-register : registry.register("formula2", _make_provider)
│   │   ├── provider.py      # Formula2Provider — délègue à Formula2Source
│   │   ├── source.py        # Formula2Source (ABC) — get_season(year)
│   │   └── sources/
│   │       ├── __init__.py  # ✅ source_registry.register("formula2", "f1calendar", ...)
│   │       └── f1calendar.py # ✅ F1CalendarSource(F1CalendarBaseSource, Formula2Source) — config F2 uniquement
│   ├── formula3/
│   │   ├── __init__.py      # ✅ auto-register : registry.register("formula3", _make_provider)
│   │   ├── provider.py      # Formula3Provider — délègue à Formula3Source
│   │   ├── source.py        # Formula3Source (ABC) — get_season(year)
│   │   └── sources/
│   │       ├── __init__.py  # ✅ source_registry.register("formula3", "f1calendar", ...)
│   │       └── f1calendar.py # ✅ F1CalendarSource(F1CalendarBaseSource, Formula3Source) — config F3 uniquement
│   ├── f1_academy/
│   │   ├── __init__.py      # ✅ auto-register : registry.register("f1-academy", _make_provider)
│   │   ├── provider.py      # F1AcademyProvider — délègue à F1AcademySource
│   │   ├── source.py        # F1AcademySource (ABC) — get_season(year)
│   │   └── sources/
│   │       ├── __init__.py  # ✅ source_registry.register("f1-academy", "f1calendar", ...)
│   │       └── f1calendar.py # ✅ F1CalendarSource(F1CalendarBaseSource, F1AcademySource) — config F1A uniquement
│   ├── __main__.py          # ✅ python -m motorsport_calendar — fallback quand Scripts pas dans PATH
│   ├── support_series/      # ✅ Framework partagé F2/F3/Academy/Supercup
│   │   ├── __init__.py      # package (non-provider)
│   │   └── f1calendar_base.py # F1CalendarBaseSource + _build_session — toute la logique HTTP/cache/mapping
│   └── wec/
│       ├── __init__.py      # ✅ auto-register : registry.register("wec", _make_provider)
│       ├── provider.py      # ✅ WecProvider — délègue à WecSource
│       ├── source.py        # ✅ WecSource (ABC) — get_season(year)
│       └── sources/
│           ├── __init__.py
│           └── official.py  # 🔴 STUB — raise NotImplementedError
│
├── config/
│   ├── __init__.py          # export ConfigService + tous les modèles
│   ├── models.py            # ✅ AppConfig, CacheConfig, IcsConfig, ProvidersConfig, ProviderConfig
│   └── service.py           # ✅ ConfigService — lit config.yaml, merge avec défauts Pydantic
│
├── core/
│   ├── __init__.py          # export ProviderRegistry + registry + SourceRegistry + source_registry
│   ├── datasource/          # ✅ Data Acquisition Layer — interfaces abstraites
│   │   ├── __init__.py      # export DataSource, JsonDataSource, HtmlDataSource, IcsDataSource
│   │   ├── base.py          # DataSource(ABC) — marqueur commun
│   │   ├── json_source.py   # JsonDataSource — fetch_json(url, params) → list | dict
│   │   ├── html_source.py   # HtmlDataSource — fetch_html(url) → str
│   │   └── ics_source.py    # IcsDataSource  — fetch_ics(url) → str
│   ├── registry.py          # ✅ ProviderRegistry — register/get/list_all/enabled/discover
│   ├── source_registry.py   # ✅ SourceRegistry — register/get/list_for/list_all/discover
│   └── service.py           # ⚠️ CalendarService — implémentée mais orpheline, aucun appelant réel (bug corrigé Sprint 50)
│
├── exporters/
│   ├── base.py              # Exporter (ABC) — export / export_to_string
│   └── ics.py               # ✅ IMPLÉMENTÉ — RFC 5545, 1 VEVENT par Session
│
├── cli.py                   # Typer CLI — generate-f1, generate-f2, generate-f3, generate-f1-academy, generate-wec (--refresh), providers, generate, export (stub)
└── utils/logging.py         # 🔴 NON IMPLÉMENTÉ — placeholder
```

---

## Fonctionnalités terminées

1. **Modèles métier** — `Championship`, `Circuit`, `Session`, `Event`, `SessionType`, `EventStatus`, tous frozen
2. **IcsExporter** — génère un fichier `.ics` valide RFC 5545, 1 VEVENT par session
3. **Formula1Provider** — délègue à `Formula1Source`, injecte le championship
4. **OpenF1Source** — appels HTTP réels via `httpx`, mapping complet, 25 circuits IANA, gestion sessions incomplètes
5. **CLI `generate-f1`** — `motocal generate-f1 YEAR OUTPUT.ics [--refresh]`, gestion erreurs HTTP
6. **HttpCache** — cache disque JSON centralisé, TTL configurable, indépendant de httpx, `--refresh` pour bypass
7. **WecProvider** — architecture identique à F1 (`WecSource` ABC + `OfficialWecSource` stub), `ChampionshipCategory.ENDURANCE`
8. **ConfigService** — lit `config.yaml` (CWD puis `~/.config/…`), valeurs par défaut Pydantic, validation automatique
9. **IcsExporter** — ajout VALARM configurable via `alarm_minutes` (0 = désactivé)
10. **ProviderRegistry** — `register/get/list_all/enabled/discover`, auto-enregistrement à l'import de `__init__.py`
11. **SourceRegistry** — `register/get/list_for/list_all/discover`, clé `(championship, source_name)`, auto-enregistrement dans `sources/__init__.py`
12. **CLI `generate`** — `motocal generate YEAR OUTPUT.ics [--refresh]` — agrège tous les providers activés en un seul ICS, résilience partielle (provider qui échoue → ✗ résumé, les autres continuent), tri chronologique
13. **`JolpicaSource`** — données F1 historiques depuis 1950 via `api.jolpi.ca` (successeur Ergast Apache-2.0). `ErgastSource` = alias backward-compat. Enregistrée sous `"jolpica"`.
14. **Data Acquisition Layer** — `core/datasource/` : `DataSource`, `JsonDataSource`, `HtmlDataSource`, `IcsDataSource`. `OpenF1Source` migrée vers `JsonDataSource` (concept validé).
15. **Formula2Provider + F1CalendarSource** — support complet FIA F2 via JSON MIT (f1calendar.com). Sessions : FP (45 min), Qualifying (30 min), Sprint Race (45 min), Feature Race (65 min). 23 circuits IANA. CLI `generate-f2 YEAR OUTPUT.ics`. Auto-inclus dans `generate`.
16. **Support Series Framework** — `F1CalendarBaseSource(JsonDataSource, ABC)` dans `providers/support_series/f1calendar_base.py`. 4 propriétés abstraites (`_series_key`, `_session_map`, `_circuit_data`, `_make_championship`). Tout le reste hérité. F3/Academy/Supercup = ~15 lignes chacun.
17. **Packaging v0.2.0** — `pyproject.toml` : version 0.2.0, `typer>=0.12` (plus `[all]`), `python-dateutil` supprimé, Python 3.14 classifié. `__main__.py` : `python -m motorsport_calendar` opérationnel. CI : matrix Ubuntu + macOS + Windows × Python 3.12/3.13.
18. **Formula3Provider + F1CalendarSource** — support complet FIA F3 via JSON MIT (f1calendar.com). Sessions : Free Practice (45 min), Qualifying (30 min), Sprint Race (30 min), Feature Race (40 min). Clés dataset F3 : `practice`, `qualifying`, `sprint`, `feature` (≠ F2). 13 circuits IANA (2021-2025). CLI `generate-f3 YEAR OUTPUT.ics`. Auto-inclus dans `generate`. Couverture 2022+.
19. **F1AcademyProvider + F1CalendarSource** — support complet F1 Academy via JSON MIT. Slug dataset : `f1-academy`. Sessions : fp1/fp2/qualifying1/[qualifying2]/race1/race2/race3. 15 circuits IANA (2023-2025). CLI `generate-f1-academy YEAR OUTPUT.ics`. Couverture 2023+. Mapping contraint (ADR-016) : race2 → FP3 pour éviter collision d'UIDs ICS sans modifier les modèles métier.
20. **Dataset Reality Check (QA-03)** — Bug critique corrigé : `F1CalendarBaseSource` utilisait `raw.get("events", [])` alors que le dataset `sportstimes/f1` utilise `"races"`. F2/F3/F1 Academy retournaient 0 événements en production depuis Sprint 14. Correction : `raw.get("races", [])`. Fixtures de tests corrigées (`"events"` → `"races"` dans 5 fichiers). Fixtures réelles ajoutées dans `tests/fixtures/real/` (F2, F3, F1A — 2 événements chacun). 16 nouveaux tests (3 dans `TestRacesKeyRegression` + 13 dans `test_real_fixtures.py`).
21. **GUI Desktop Phase 1 (Sprint 22 + Hotfix GUI-01 + GUI-02)** — Package `motorsport_calendar/gui/` : `models.py` (GenerateState), `controller.py` (list_championships + async generate_calendar), `main_view.py` (Flet UI), `app.py` (ft.run), `__main__.py`. Dépendance optionnelle `flet>=0.80`. Entrée script `motocal-gui`. 32 tests GUI (12 models + 20 controller, sans Flet). Flet 0.85 : `ft.run()`, `ft.Button(content=str)` (plus `text=`), `ft.Icons`, `ft.Colors`, `FilePicker.save_file()` async, `Dropdown(on_select=)` (plus `on_change=`), `FilePicker` dans `page.services` (plus `page.overlay` — hérite de `Service` et non `Control`).
22. **GUI Desktop Alpha 2 — UX Polish (Sprint 23)** — `strings.py` (centralisation textes UI, `Strings.from_dict()` pour i18n future, `plural(n)`), `display_names.py` (mapping IDs → noms lisibles, `DEFAULT_SELECTED`), `preferences.py` (persistance dans `~/.config/motorsport-calendar/gui_prefs.json`), `assets/` (placeholder icône). `controller.generate_calendar()` retourne `tuple[int, int]` (events, sessions) au lieu de `int`. Dialogue succès : `page.show_dialog()` / `page.pop_dialog()`. Nom de fichier pré-rempli `motorsport-calendar-{year}.ics`. 36 nouveaux tests. 695 tests total.
24. **Release Alpha Phase 1 (Sprint 25)** — Architecture `gui/views/` : 5 modules indépendants (`weekend.py`, `calendar.py`, `favorites.py`, `preferences.py`, `about.py`), chacun exposant `build_*_view()`. `CalendarViewControls` dataclass pour injecter les contrôles dans `calendar.py`. `PreferencesModel` frozen dataclass (language, timezone, first_day_of_week, favorite_championships, preferred_calendar, bapps_sync_enabled). `main_view.py` refactorisé en shell de navigation pur. Navigation 5 destinations : Ce week-end / Mon calendrier / Mes favoris / Préférences / À propos. Flet fix bonus : `ft.Border.all()` (pas `ft.border.all()`). 44 nouveaux tests. **764 tests total**.
23. **GUI Desktop Alpha 3 — Product Polish (Sprint 24)** — `categories.py` (`Category` StrEnum 5 valeurs, `ChampionshipGroup` frozen dataclass, `GROUPS` registre, `get_groups_for()` helper avec fallback). Navigation `ft.NavigationRail` 3 destinations (Accueil/Calendrier/À propos). `page.on_resize` pour rail étendu >900px. Championnats groupés visuellement (`🏎 Formula`, `🏁 Endurance`). Écran Accueil + écran À propos (lien GitHub via `ft.UrlLauncher`). `strings.py` + 11 chaînes (nav + about). 25 nouveaux tests. **720 tests total**. Services Flet : `FilePicker` + `UrlLauncher` dans `page.services`.
25. **Release Alpha Phase 2 — UX & Design System (Sprint 26)** — `gui/theme.py` (nouveau) : `BAppsColors`/`MotorsportColors` (palettes brutes, sourcées depuis `BApps-Studio/02-Brand/BrandGuide.md` et `.../Motorsport-Calendar/Branding/Branding.md`), `Colors` (rôles sémantiques — seule couche que les vues doivent importer), `Spacing`/`Radius`/`IconSize`/`FontSize` (échelles), `page_padding()`/`section_title()`/`button_style()`/`card()`/`chip()`/`logo_placeholder()`. "Mon calendrier" (`gui/views/calendar.py`) réécrit en assistant 4 étapes (saison/championnats/destination/créer) : `CalendarViewControls` étendue (`current_step`, `on_step_click`, `back_btn`, `next_btn`, `recap_controls`), un seul step rendu à la fois, layout pur (aucun état). `GenerateState` (`models.py`) : `current_step`, `STEP_COUNT=4`, `step_valid(step)`, `can_advance()`, `can_go_back()` — logique de wizard testable sans Flet. `main_view.py` : handlers `on_wizard_next/back`, `on_step_click`, `_refresh_calendar_view()` (seule vue reconstruite à chaque changement — toutes les autres restent "construites une fois, swap sans rebuild"), `_build_recap_controls()` (récap étape 4 avec liens "Modifier"). `weekend.py`/`favorites.py`/`preferences.py`/`about.py` : reskin visuel via `theme.*` uniquement, contenu/comportement inchangés. `gui/assets/logo/README.md` : emplacement du futur logo (Brand Set v1.0 validé mais SVG non livrés ce sprint) — `theme.logo_placeholder()` en attendant, dans `about.py` et l'en-tête du wizard. Correction incidente : `ft.Colors.WHITE12/30/38/54/60/70` dépréciés en Flet 0.85 → `WHITE_12/30/38/54/60/70`, corrigé une seule fois dans `theme.py`. 55 nouveaux tests (34 `test_gui_theme.py` + 15 wizard state + 6 wizard views). **819 tests total**.
26. **Uniformisation du layout (Sprint 27)** — `theme.page_shell(*sections)` (nouveau) : LA grille unique de toutes les vues — `Container` externe `expand=True, alignment=TOP_CENTER` (centre uniquement le conteneur) + `Container` interne `width=MAX_CONTENT_WIDTH` (1000, se rétrécit naturellement sous 1000px via `BoxConstraints.enforce` de Flutter, aucun recalcul manuel au resize) + `Column` unique `horizontal_alignment=STRETCH` (remplit la largeur des cartes/formulaires, ne centre jamais le contenu). Les 5 vues (`weekend.py`, `calendar.py`, `favorites.py`, `preferences.py`, `about.py`) construisent leurs sections (`section_title` + `Divider` + contenu) et les passent à `page_shell(...)` au lieu de bâtir leur propre conteneur racine. Weekend/Favoris/À propos perdent leur centrage (`TOP_CENTER`/`CrossAxisAlignment.CENTER`/`text_align=CENTER`) au profit d'un contenu aligné à gauche, comme Mon calendrier/Préférences l'étaient déjà. Mon calendrier perd son en-tête custom (logo+nom app) au profit du même `section_title` que les autres pages (nom de section, pas nom de l'app) — le placeholder logo (Sprint 26) reste uniquement sur À propos. Carte placeholder Ce week-end : largeur fixe (320px) supprimée, remplit le gabarit comme les autres cartes. `GenerateState`, `CalendarViewControls` et les handlers de `main_view.py` **inchangés** — uniquement le conteneur racine retourné par chaque `build_*_view()`. `TestAllViewsShareTheSameGrid` verrouille que les 5 vues partagent strictement la même largeur/centrage/alignement/padding. 18 nouveaux tests. **837 tests total**.
27. **Uniformisation finale de l'interface (Sprint 28)** — Wizard : `_step_header()` supprimé de `gui/views/calendar.py`, plus de titre "Étape N — ..." ni de texte d'aide sous le bandeau de pastilles — chaque étape commence directement par son champ (`_step_season` = juste `year_dropdown`, etc.). 8 constantes `wizard_title_*`/`wizard_help_*` supprimées de `strings.py` (`wizard_step_*` conservées). Pages vides (`weekend.py`, `favorites.py`, `preferences.py`) : passent désormais **une seule section** à `page_shell(...)` — un `theme.card(...)` contenant tout leur contenu (en-tête `section_title` + `Divider` + corps). Sous-éléments qui avaient leur propre bordure (`_race_preview_content` dans weekend.py, `_pref_row` dans preferences.py) l'ont perdue pour éviter un double encadrement, maintenant nested dans la carte centrale unique de la page. À propos (`about.py`) : header générique `section_title("À propos")` retiré — le bloc "Motorsport Calendar / Version Alpha" (+ logo placeholder) sert lui-même de titre ; espaceurs `Container(height=...)` manuels remplacés par l'espacement uniforme de `page_shell`. Pas de carte pour À propos (contrairement aux 3 pages vides). `theme.py`, `main_view.py`, `models.py`, navigation, couleurs, Design Tokens **non modifiés**. 9 nouveaux tests. **846 tests total**.
28. **Ce week-end — version fonctionnelle (Sprint 29)** — `gui/upcoming_weekend.py` (nouveau, logique pure) : `WeekendEntry(championship_id, event)` (voir ADR-020 — `Event.championship.id` est suffixé par l'année, jamais l'id du registre), `find_next_weekend_entries()` (avance semaine par semaine, vendredi-dimanche UTC, jusqu'à trouver ≥1 session), `find_upcoming_weekend()` (recherche + regroupement via `categories.get_groups_for` réutilisé tel quel + mise en forme FR : `_SESSION_LABELS`, `_COUNTRY_LABELS` ~35 pays, heure convertie dans `Circuit.timezone`). `gui/controller.py` : `get_upcoming_weekend(now=None)` — mirroir exact de `generate_calendar` (mêmes registries/cache, `refresh=False` toujours, aucun nouveau provider), boucle sur les 5 championnats fixes (`WEEKEND_CHAMPIONSHIP_IDS`) × 2 années. `gui/views/weekend.py` réécrit : `build_weekend_view(result: WeekendResult | None)` — 3 états exacts (chargement/aucune course/trouvé, une carte par championnat). `main_view.py` : `weekend_container` + `_load_weekend()` tâche asyncio démarrée une fois au lancement (référencée sur `page.weekend_load_task`, jamais retapée par visite d'onglet), résolue via le cache HttpCache existant (TTL 24h). 34 nouveaux tests (21 logique pure + 8 contrôleur + 5 vue). **880 tests total**.
29. **Composant ChampionshipCard (Sprint 30)** — nouveau paquet `gui/components/` (bibliothèque de composants, distincte de `gui/views/` et de `gui/theme.py`). `components/championship_card.py` : `SessionRow`/`ChampionshipCardData` (modèle du composant, promu depuis `upcoming_weekend.WeekendCard`/`SessionRow` — renommage, mêmes champs) + `build_championship_card(data, *, footer=None)`. En-tête sur 4 lignes distinctes (championnat, Grand Prix, circuit, pays — plus de "·" combinant circuit/pays). Sessions alignées via `ft.Row(alignment=SPACE_BETWEEN)` (libellé à gauche, heure à droite, indépendant de la longueur du texte). `footer` : point d'extension pour Favori/Notifications/Export ICS/Partage/Résultats — `None` aujourd'hui partout, aucun changement visuel ; quand fourni, ajoute `Divider` + le contrôle sans l'interpréter. `upcoming_weekend.py` et `views/weekend.py` migrés pour consommer ce composant partagé (`_found_state` ne construit plus aucun layout de carte). 23 nouveaux tests. **903 tests total**.
30. **Layout System (Sprint 31)** — nouveau sous-paquet `gui/components/layout/` : `PageContainer(*, header=None, body=())` (délègue à `theme.page_shell`, largeur/padding/alignement), `PageHeader(title, *, icon=None, subtitle=None)` (icône+titre+sous-titre+séparateur, remplace `theme.section_title()+ft.Divider()` partout), `Section(*controls, spacing=Spacing.SM)` (espacement entre blocs, aucune connaissance des titres), `SectionHeader(title, *, icon=None)` (intitulé secondaire dans une Section, sans consommateur actuel — prêt pour Tableau de bord/Recherche), `CardList(cards, *, spacing=Spacing.SM)` (liste verticale uniforme), `EmptyState(title, *, message=None, icon=None)` (le "rien ici" encarté, centralisé), `PageSpacing(size=Spacing.MD)` (espace nommé ponctuel). Nommage PascalCase délibéré (widget-style) — exception `ruff.toml` `N802` scopée à ce seul paquet. Les 5 vues migrées : `weekend.py`/`favorites.py`/`preferences.py`/`calendar.py` utilisent `PageHeader` séparé du corps (changement assumé vis-à-vis du Sprint 28 — voir ADR-022) ; `about.py` garde son bloc de marque compact SANS `PageHeader` (choix Sprint 28 préservé). Préférences : les lignes retrouvent leur bordure individuelle (plus de carte englobante à éviter de doubler). 51 nouveaux tests (`test_gui_components_layout.py`) + 11 tests `test_gui_views.py` adaptés/ajoutés. **957 tests total**.
31. **Normalisation des métadonnées d'événement (Sprint 32)** — nouveau module `gui/event_display.py` : `EventDisplayData(grand_prix_name, circuit_name: str | None, country: str | None)` + `normalize_event_display(championship_id, event)`. Corrige le doublon "Belgian / Belgian" observé sur F2/F3/F1 Academy (voir investigation ci-dessus) et le "Unknown" affiché tel quel : (1) doublon Grand Prix/circuit → une seule ligne, (2) circuit inconnu → repli `circuit.name`→`circuit.city`→masqué, (3) pays `"Unknown"`/vide → masqué, jamais affiché, (4) nom absent → suffixe " Grand Prix" ajouté pour F1/F2/F3/F1 Academy (jamais WEC) puis repli circuit puis `STRINGS.event_name_fallback` ("Événement"). `country_label()` déménagé depuis `upcoming_weekend.py`. `ChampionshipCardData.circuit_name`/`.country` deviennent `str | None` ; `build_championship_card` omet simplement une ligne `None`, sans aucune décision (toujours zéro logique métier dans le composant). 26 nouveaux tests. **983 tests total**.
32. **Registre des identités visuelles de championnat (Sprint 33)** — nouveau module `gui/championship_assets.py` : `ChampionshipAsset(logo_src: str | None)` + `get_championship_asset(championship_id)`, point d'entrée unique. Recherche préalable dans `motorsport-calendar` ET `BApps-Studio` : aucun logo officiel de championnat n'existe nulle part dans le projet — clarifié avec l'utilisateur, option retenue = registre "prêt à recevoir" (même patron que `gui/assets/logo/README.md`). `logo_src` résolu seulement si le fichier existe réellement sur `_ASSETS_DIR / filename` ; id inconnu et id sans fichier livré → `None`, indistinguables côté appelant, jamais d'exception. `ChampionshipCard._header_title()` : `ft.Text` nu si `None` (état réel de tous les championnats aujourd'hui — layout strictement inchangé), sinon `ft.Row([ft.Image(logo_src, width=height=IconSize.LG, fit=BoxFit.CONTAIN), ft.Text(...)])` — aucun `if championship_id == ...` dans le composant. Nouveau dossier `gui/assets/championships/` (`.gitkeep` + README), aucun fichier logo livré. Piège Flet 0.85.3 : `ft.ImageFit` n'existe pas, c'est `ft.BoxFit`. 16 nouveaux tests. **999 tests total**.
33. **Extension Formula — Formula E (Sprint 34)** — nouveau `providers/formula_e/` : `FormulaEProvider`/`FormulaESource` (ABC) + `sources/f1calendar.py` réutilisant `F1CalendarBaseSource` telle quelle (`_series_key="fe"`, `_SESSION_MAP` avec `practice1/practice2/practice3/qualifying/race`, `_CIRCUIT_DATA` 16 circuits 2023-2025). Contrairement au triple-header F1 Academy, un week-end Formula E double-header est déjà **deux rounds séparés** dans le dataset (chacun avec son propre numéro de round et sa propre session `race`) — aucun contournement d'UID nécessaire. Le brief citait par erreur "F1 Academy" comme manquant ; vérifié déjà entièrement intégré depuis le Sprint 29+, aucune modification. `cli.py` : les 5 commandes `generate-f1/f2/f3/f1-academy/wec` (copier-coller quasi identique depuis leur création) factorisées vers `_run_generate_command()` — sortie/erreurs strictement identiques (verrouillé par les tests existants), `generate-formula-e` ajoutée sur ce même helper. Wizard/"Ce week-end"/catégories entièrement génériques (`registry.list_all()` → `categories.get_groups_for()` → `display_names.get_display_name()`) : seules 3 lignes ajoutées (`categories.py`, `display_names.py`, `upcoming_weekend.py::WEEKEND_CHAMPIONSHIP_IDS`), zéro changement dans `main_view.py`/`calendar.py`. 46 nouveaux tests. **1045 tests total**.
34. **Extension Endurance — ELMS + Michelin Le Mans Cup (Sprint 35)** — nouveau `providers/aco_series/sports_event_base.py` : `AcoSportsEventSource(HtmlDataSource, ABC)`, abstraction découverte (pas anticipée) après avoir constaté en direct que fiawec.com/europeanlemansseries.com/lemanscup.com tournent sur le même CMS ACO — chaque page course (`/en/race/{slug}`) embarque un `<script type="application/ld+json">` schema.org `SportsEvent`/`subEvent` avec horaires ISO 8601 exacts (aucune API publique, mais bien plus fiable qu'un scraping HTML classique). `providers/elms/` et `providers/mlmc/` consomment cette base, ne déclarant que 4 propriétés spécifiques (`_series_key`, `_base_url`, `_event_name_prefix`, `_circuit_data` — ce dernier partagé entre ELMS/MLMC via `aco_series/circuit_data.py`, les deux séries étant co-localisées sur les 6 mêmes circuits 2026, constaté en direct). Road to Le Mans n'est pas un championnat séparé — elle apparaît comme un round de plus dans `mlmc`, fidèle au site officiel. Deux subtilités gérées génériquement (pas par série) : (1) qualifs multi-classes (plusieurs créneaux de 25 min consécutifs, un par classe LMP2/LMP3/LMGT3) fusionnées en une seule Session par type — évite la collision d'UID (`{event_uid}-{session.type}`) sans le contournement à la F1 Academy (relabelling vers un SessionType non lié) ; (2) jours de tests pré-saison ("Official Tests"/"Collective Tests") exclus de la liste des rounds — pas des manches du championnat, et leur structure de sessions ("Morning/Afternoon Session" répété sur plusieurs jours) collisionnerait de toute façon. Bug réel détecté en vérification live (pas en test unitaire) : la durée de course "Road to Le Mans" calculée à partir de l'`endDate` de l'événement donnait +61h au lieu de ~3h (l'`endDate` couvre toute la semaine des 24H du Mans, pas seulement la course RTLM) — corrigé par un plafond de plausibilité (`_MAX_PLAUSIBLE_RACE_DURATION`, 26h). Autre bug détecté en vérification live : `fetch_html` devait être rendu transparent au cache (même contrat que `F1CalendarBaseSource.fetch_json`) après qu'un mock posé dessus se soit avéré insuffisant pour contourner le cache disque réel — corrigé, sans impact sur le comportement observable. CLI : `generate-elms`/`generate-mlmc` ajoutées sur `_run_generate_command()` (Sprint 34). Wizard/"Ce week-end"/catégories : 3 lignes ajoutées (groupe "Endurance" désormais `("wec", "elms", "mlmc")`), zéro changement de vue. Nouvelle dépendance `beautifulsoup4`/`lxml` (parsing HTML/JSON-LD — `core/datasource/html_source.py` anticipait déjà cette classe d'implémentation). 105 nouveaux tests, dont des fixtures réelles non retouchées dans `tests/fixtures/real/`. **1150 tests total**.
35. **Extension IMSA — sortie de l'écosystème ACO (Sprint 36)** — premier championnat organisé par une entité totalement extérieure à l'ACO (IMSA WeatherTech SportsCar Championship), objectif explicite de valider que l'architecture Provider/Source généralise au-delà des sources déjà connues. Investigation exhaustive de source avant toute écriture de code : aucune API publique documentée ; imsa.com bloqué au niveau infrastructure (Cloudflare, HTTP 403 + `cf-mitigated: challenge` sur toutes les routes testées — page d'accueil, calendrier, articles, PDF statiques — contournable seulement via automatisation navigateur complète, hors de portée) ; le prestataire de chronométrage Al Kamel Systems (même que WEC/ELMS/MLMC) n'expose qu'une archive de résultats post-course, pas de calendrier prévisionnel ; Wikipedia (API MediaWiki) donne un tableau de calendrier propre mais sans horaires de session ; Sportscar365 publie des horaires mais uniquement en prose libre dans des articles, non structuré. Décision confirmée avec l'utilisateur (AskUserQuestion, 3 options présentées) : stub à la WEC — `providers/imsa/` enregistré et intégré partout (registry, wizard, "Ce week-end", agrégateur, catégories, noms lisibles) exactement comme les 8 championnats précédents, `OfficialImsaSource.get_season` lève `NotImplementedError`, aucun horaire inventé. Aucun provider existant modifié. CLI `generate-imsa` ajoutée sur `_run_generate_command()` (Sprint 34), mirroring exact de `generate-wec`. 39 nouveaux tests (`test_imsa_provider.py`/`test_cli_generate_imsa.py`, mirroring exact des suites WEC), zéro régression sur les 1150 tests existants. **1189 tests total**.
36. **GT Racing — GT World Challenge Europe/America/Asia, IGTC (Sprint 37)** — nouveau `providers/sro_series/timetable_base.py::SroTimetableSource(HtmlDataSource, ABC)`, abstraction découverte (pas anticipée) après avoir constaté en direct que les quatre sites `.com` de SRO Motorsports Group tournent sur le même CMS — chaque page course (`/event/{id}/{slug}`) expose un tableau HTML `<table class="timetable__table">` par jour (colonnes Session / Local Time / GMT). Aucune API publique, aucun JSON-LD (contrairement à WEC/ELMS/MLMC) — scraping HTML classique en dernier recours, conformément à la consigne du sprint. `providers/gtwc_europe/`, `gtwc_america/`, `gtwc_asia/`, `igtc/` consomment cette base, ne déclarant que 2 propriétés spécifiques (`_series_key`, `_base_url`) plus `_make_championship()` — la table de circuits (`sro_series/circuit_data.py`, nom propre + fuseau IANA, clé = slug d'URL) et la logique de mapping de sessions sont entièrement partagées, la co-localisation d'un même slug de circuit entre plusieurs sites (`crowdstrike-24-hours-of-spa` identique sur gt-world-challenge-europe.com et intercontinentalgtchallenge.com) étant constatée en direct, pas supposée. Format double manche "Sprint Cup" (GTWC Europe/Asia — 2 Qualifying + 2 Race par week-end) détecté dynamiquement par comptage des sessions "Race" (jamais supposé fixe) : une seule Race → QUALIFYING/RACE classique ; deux Race → la première chronologiquement devient SPRINT_QUALIFYING/SPRINT (même mécanisme que les week-ends Sprint F1), la seconde reste QUALIFYING/RACE — les entrées Qualifying sont ensuite réparties selon la Race qu'elles précèdent chronologiquement. Practice en surnombre (Bathurst 12 Hour : 6 séances Free Practice numérotées, alors que le modèle n'a que 3 emplacements FP1/FP2/FP3) fusionné génériquement : les deux premières séances mappent normalement, tout ce qui suit la 3ème est fusionné dans une seule session FP3 (span étendu) plutôt que perdu. Aucune heure de fin fournie par la source (contrairement au JSON-LD ACO) : durée de course inférée depuis le motif "N Hour(s)" dans le slug d'URL de l'événement (`bathurst-12-hour` → 12h, `crowdstrike-24-hours-of-spa` → 24h), repli sur une durée par défaut pour les formats non explicites (ex. `suzuka-1000km`, approximation assumée). Bug réel détecté en vérification live (pas en test unitaire) : combiner la date locale de la légende du tableau directement avec l'heure de la colonne GMT produisait un jour UTC incorrect pour les circuits loin de l'UTC (confirmé sur Bathurst/Sydney : une séance du vendredi matin local se retrouvait calculée sur le jeudi UTC, pas le vendredi) — corrigé en calculant le véritable instant UTC à partir de l'écart entre les colonnes "Local Time" et "GMT" de chaque ligne, sans dépendre d'aucune base de fuseaux horaires externe. CLI : `generate-gtwc-europe`/`generate-gtwc-america`/`generate-gtwc-asia`/`generate-igtc` ajoutées sur `_run_generate_command()` (Sprint 34). Nouvelle catégorie GUI `Category.GT` (groupe "🚗 GT"), `elms`/`mlmc`/`wec`/`imsa` restent dans "Endurance" — aucun changement de vue. 184 nouveaux tests, dont des fixtures réelles non retouchées dans `tests/fixtures/real/` (calendrier + une page course par site). **1373 tests total**.
37. **Motorcycle Racing — MotoGP, Moto2, Moto3, WorldSBK (Sprint 38)** — nouveau `providers/motogp_series/pulselive_base.py::PulseliveGpSource(JsonDataSource, ABC)`, abstraction découverte (pas anticipée) après avoir constaté en direct que MotoGP/Moto2/Moto3 courent le même week-end de Grand Prix, au même circuit, et sont exposés par **l'API REST officielle et non authentifiée de Dorna Sports** (`api.pulselive.motogp.com/motogp/v1/events?seasonYear={year}`) — une seule requête par saison couvre les trois classes, le tableau `broadcasts` de chaque round embarquant déjà toutes les sessions taguées par `category.acronym` (MGP/MT2/MT3), avec de vraies heures de début ET de fin (contrairement à SRO, Sprint 37). Aucun scraping — c'est la meilleure source trouvée depuis le début du projet en termes de qualité de données brutes. `providers/motogp/`, `moto2/`, `moto3/` consomment cette base, ne déclarant que 3 propriétés spécifiques (`_series_key`, `_category_acronym`, `_race_duration_minutes`) plus `_make_championship()`. Deux subtilités gérées génériquement (pas par classe) : (1) chaque classe court 3 séances PRACTICE-kind par week-end, mais seules 2 portent un numéro explicite (FP1/FP2) — la 3ème s'appelle juste "PR" ("Practice") et tombe chronologiquement ENTRE les deux ; plutôt que de faire confiance au libellé, les 3 séances sont triées chronologiquement et assignées FP1/FP2/FP3 par ordre de créneau (le libellé source reste visible dans `Session.title`, donc l'écart entre "mon FP2" et "le FP2 de la source" n'est jamais caché) ; (2) Qualifying tourne en deux segments (Q1/Q2) par classe, fusionnés en une seule session QUALIFYING allant du début de Q1 à la fin réelle de Q2 (heures réelles fournies par la source, contrairement à ACO/SRO qui devaient inventer une durée). Seules les sessions RACE/SPRINT (Sprint MotoGP uniquement) n'ont pas d'heure de fin réelle (`date_start == date_end` systématiquement) — durée par défaut documentée par classe/format. Bug réel détecté en vérification live (pas en test unitaire) : les horodatages de la source portent le décalage UTC local du circuit (ex. `+07:00`), pas UTC — sans normalisation, `IcsExporter` produisait un `DTSTART;TZID="UTC+07:00"` synthétique sans `VTIMEZONE` correspondant ; corrigé en normalisant chaque horodatage vers UTC dans `_parse_datetime`, cohérent avec tous les autres providers du projet. `providers/worldsbk/` enregistré avec un stub à la WEC/IMSA après investigation exhaustive (même famille de plateforme "Pulse Live" confirmée, mais calendrier entièrement rendu côté client et hôte API candidat injoignable depuis l'extérieur) — décision confirmée avec l'utilisateur (AskUserQuestion). CLI : `generate-motogp`/`generate-moto2`/`generate-moto3`/`generate-worldsbk` ajoutées sur `_run_generate_command()` (Sprint 34). Nouveau groupe GUI "🏍 Moto" réutilisant `Category.MOTO`, déjà anticipée dans l'énumération depuis le Sprint 37 (docstring de `categories.py` prédisait exactement ce scénario). 166 nouveaux tests, dont une fixture réelle non retouchée (`tests/fixtures/real/motogp_events_2026.json`). **1539 tests total**.
38. **Dashboard Motorsport — page d'accueil (Sprint 39)** — premier sprint "valeur" du projet : aucun nouveau provider, aucune nouvelle source de données, uniquement de l'UX exploitant les 17 championnats déjà intégrés. Nouveau `gui/dashboard.py` (logique pure, sans Flet, mirroring exact de `upcoming_weekend.py` — "fetch" dans `controller.py`, "compute" dans ce module) expose `DashboardData`/`NextRaceStart`/`build_dashboard_data()`. Le pipeline de fetch (`registry`/`source_registry`/`HttpCache`, boucle sur les 17 championnats "Ce week-end" pour l'année courante + suivante) était dupliqué en germe entre `get_upcoming_weekend` et ce nouveau besoin — extrait en `controller._fetch_weekend_entries()` (comportement strictement préservé, vérifié par la suite de tests existante intégralement verte après refactor) plutôt que dupliqué, cohérent avec le principe du sprint ("mutualiser uniquement lorsqu'une abstraction apparaît naturellement", déjà appliqué aux providers depuis le Sprint 35). `controller.get_dashboard_data()` réutilise ce pipeline unique : les statistiques de saison (nombre d'événements/sessions) sont dérivées des mêmes événements déjà récupérés pour trouver le prochain week-end — aucune requête réseau supplémentaire dédiée. Nouvelle fonction publique `upcoming_weekend.format_session_datetime()` (jour + date + heure, fuseau local du circuit) pour le "prochain départ" — un stat isolé qui, contrairement à une ligne de session déjà contextualisée à un week-end connu à l'intérieur d'une ChampionshipCard, a besoin de la date complète, pas seulement du jour. "Prochain départ" est défini comme la prochaine session `SessionType.RACE` (uniquement — jamais Sprint/Hyperpole) toutes classes confondues, à partir de maintenant. Nouvelle vue `gui/views/dashboard.py`, entièrement composée du Layout System existant (`PageContainer`/`PageHeader`/`Section`/`SectionHeader`/`EmptyState`) plus `theme.card()`/`theme.chip()` pour les stats et les puces de championnats — aucun nouveau composant, aucun nouveau token de Design System, conforme à la consigne "respecter intégralement le Design System… aucun travail sur les icônes… les placeholders restent utilisés" (aucun asset touché). Devient la page d'accueil : premier onglet de la barre de navigation (les 5 onglets existants décalés d'un cran), chargé en arrière-plan au lancement via le même pattern que "Ce week-end" (Sprint 29) — `page.dashboard_load_task`. 32 nouveaux tests. **1571 tests total**.
39. **Calendrier interactif — "Mon calendrier" devient un navigateur (Sprint 40)** — deuxième sprint "valeur" : le wizard de génération ICS 4 étapes (saison/championnats/destination/créer, inchangé depuis le Sprint 26) reste identique, mais gagne un résumé persistant (événements/sessions sélectionnés, période couverte) visible sur les 4 étapes, pas seulement au récapitulatif final — filtrer par année/championnat donne désormais un retour immédiat, transformant la page de "remplir un formulaire" à "explorer avant de générer". Nouveau `gui/calendar_selection.py` (logique pure, sans Flet, sans I/O, mirroring exact de `dashboard.py`/`upcoming_weekend.py`) : `SelectionSummary(event_count, session_count, period_start, period_end)` + `build_selection_summary(year_events, selected_championships)` — agrège localement les événements déjà récupérés pour la sélection courante ; `period_start`/`period_end` sont `None` exactement quand `event_count == 0` (jamais de résumé à moitié rempli). `controller.get_calendar_year_events(year)` (nouveau) : un seul fetch par année couvrant **tous** les championnats enregistrés (`registry.list_all()`, contrairement à `_fetch_weekend_entries` limité aux 17 championnats "Ce week-end") — cocher/décocher un championnat devient un filtrage purement local sur des données déjà en mémoire, aucune requête réseau par case cochée, seul un changement d'année en déclenche une nouvelle. Extraction `controller._resolve_source_and_provider_factories(cid, config)` : résolution championnat→source→provider partagée entre `_fetch_weekend_entries` et `get_calendar_year_events` (même justification que le Sprint 39 pour `_fetch_weekend_entries` — mutualiser seulement quand une vraie réutilisation apparaît ; `generate_calendar()` délibérément non refactorisé dessus, ses tests verrouillant des messages d'erreur exacts que le retour `None`-générique de l'aide partagée aurait perdus). `gui/views/calendar.py` : nouvelle fonction pure `_selection_summary_block(summary: SelectionSummary | None)` insérée entre l'indicateur d'étapes et le corps de l'étape dans `build_calendar_view` — trois états distincts (chargement avec `ProgressRing`, sélection vide avec message dédié, résumé peuplé avec compteurs + période `dd/mm/yyyy - dd/mm/yyyy`) ; `CalendarViewControls.selection_summary` nouveau champ, `None` signifiant spécifiquement "fetch de l'année en cours" et jamais "sélection vide" (un `SelectionSummary(event_count=0, ...)` résolu est un état distinct, rendu différemment). `main_view.py` : pattern de fetch en arrière-plan identique à `_load_weekend`/`_load_dashboard` (Sprints 29/39) — `year_events` en closure, `_load_year_events(year)` avec garde d'obsolescence (`if year != state.year: return`, ignore une réponse tardive pour une année que l'utilisateur a quittée), déclenché une fois au lancement et à chaque changement d'année via `on_year_change` (jamais à chaque coche de case). 25 nouveaux tests. **1596 tests total**.
40. **Explorer une saison — l'explorateur de saison (Sprint 41)** — troisième sprint "valeur" consécutif, dans la continuité directe du résumé de sélection (Sprint 40) : le wizard 4 étapes reste identique, mais gagne, sous le résumé, la liste concrète des événements de la sélection courante — nom, championnat, circuit, pays, date — triée chronologiquement et regroupée par mois. Nouveau `gui/season_explorer.py` (logique pure, sans Flet, sans I/O, mirroring exact de `calendar_selection.py`/`dashboard.py`/`upcoming_weekend.py`) : `SeasonEventRow(event_name, championship_name, circuit_name, country, date_label)` + `SeasonMonthGroup(month_label, rows)` + `build_season_explorer(year_events, selected_championships)`. Réutilise `event_display.normalize_event_display` (Sprint 32, ADR-023) pour le nom/circuit/pays — jamais réimplémenté, jamais de "Unknown" ni de ligne dupliquée ; réutilise `display_names.get_display_name` pour le nom de championnat. Le tri et le regroupement sont ancrés sur la session la plus précoce de chaque événement (jamais sur `event.season`, qui ne reflète pas toujours le mois calendaire réel — voir l'anomalie Formula E documentée au Sprint 40, désormais couverte par un test dédié), convertie en UTC — même convention que `upcoming_weekend._session_utc_date` (limitation déjà documentée pour les circuits loin de l'UTC, pas une nouvelle). Un événement sans session est exclu (aucune date à lui assigner), jamais une erreur. Aucun nouveau fetch : réutilise directement `year_events`, déjà récupéré par `controller.get_calendar_year_events` (Sprint 40) — cocher/décocher un championnat ou changer d'année recalcule la liste localement via le même `_refresh_calendar_view()` déjà en place, satisfaisant "mis à jour automatiquement" sans code supplémentaire dans `main_view.py` au-delà d'une nouvelle fonction `_current_season_groups()`, mirroir exact de `_current_selection_summary()` (même convention `None`-= chargement vs tuple vide = "rien ne correspond"). `gui/views/calendar.py` : nouvelle fonction pure `_season_explorer_block(groups)` insérée juste après `_selection_summary_block`, visible sur les 4 étapes — construite exclusivement à partir du Layout System (`Section`/`SectionHeader`/`CardList`/`EmptyState`) et de `theme.card()`, aucun nouveau composant créé (une ligne par événement est structurellement trop différente d'une `ChampionshipCard`, orientée détail de sessions d'un seul événement, pour justifier sa réutilisation) ; chaque ligne affiche nom/championnat/circuit/pays sur des lignes séparées, jamais combinées par "·" (règle déjà posée pour `ChampionshipCard` au Sprint 30, reconduite ici à l'identique). 23 nouveaux tests. **1619 tests total**.
41. **Fiche événement (Sprint 42)** — quatrième sprint "valeur" consécutif : chaque ligne de l'explorateur de saison (Sprint 41) devient cliquable, ouvrant une fiche détaillée — championnat, nom de l'épreuve, circuit, pays, date, et la liste chronologique des sessions (type + heure). Nouveau `gui/event_details.py` (logique pure, sans Flet, sans I/O, mirroring exact des autres modules "compute" du paquet) : `EventDetails(card: ChampionshipCardData, date_label: str | None)` + `build_event_details(championship_id, event)`. Prend au pied de la lettre la consigne "réutiliser les modèles existants" : `ChampionshipCardData`/`SessionRow` (composant `ChampionshipCard`, Sprint 30) ne sont jamais redéfinis, seulement peuplés — le seul champ qu'ils ne portent pas (un intitulé de date unique au niveau de l'événement, distinct de l'heure de chaque session) est ajouté via `EventDetails.date_label`, `None` exactement quand l'événement n'a aucune session. Extraction `event_display.session_type_label(session) -> str` (nouveau, public) : la table `SessionType -> libellé FR` vivait en privé dans `upcoming_weekend.py` (`_SESSION_LABELS`/`_session_type_label`) ; `event_details.py` en devenant un second consommateur réel, la table a été promue dans `event_display.py` (déjà le module canonique pour "comment présenter un événement", Sprint 32) plutôt que dupliquée une seconde fois — `upcoming_weekend.py` réutilise désormais cette même fonction, comportement strictement préservé (suite de tests existante intégralement verte après le refactor, sans modification). `gui/season_explorer.py` : `SeasonEventRow` gagne 2 champs, `championship_id`/`event_uid` — jamais l'`Event` domaine lui-même (les dataclasses de ce paquet restent "display-ready", jamais des objets domaine) — juste assez d'identité pour qu'un clic puisse retrouver l'événement dans `year_events`. `gui/views/calendar.py` : `_season_event_row`/`_season_explorer_block` acceptent un `on_click`/`on_event_click` forwardé sans interprétation (aucune logique métier dans la vue) ; `CalendarViewControls.on_event_click` nouveau champ, défaut no-op comme `on_step_click`. Clic mécanique : `theme.card(...)` retourné par `_season_event_row` reçoit `.on_click = lambda e: on_click(row)` — aucune modification de `theme.py`, aucun nouveau composant, juste l'attribut déjà supporté par tout `ft.Container`. `main_view.py` : `_on_event_row_click(row)` (recherche l'`Event` dans `year_events[row.championship_id]` par `event_uid`) + `_show_event_details_dialog(details)` (nouvelle boîte de dialogue, mirroir exact du patron déjà établi par `_show_success_dialog` — `ft.AlertDialog`/`page.show_dialog`/`page.pop_dialog`) qui réutilise `build_championship_card(details.card)` tel quel comme corps principal, n'ajoutant que la ligne `date_label` au-dessus. Aucune requête réseau supplémentaire : l'événement cliqué est toujours déjà en mémoire (`year_events`, Sprint 40). 20 nouveaux tests. **1639 tests total**.
42. **Refonte UX de "Mon calendrier" (Sprint 43)** — premier sprint purement ergonomique du projet : aucune nouvelle fonctionnalité métier, aucun nouveau provider, aucune modification de la logique métier ni des modèles de domaine, explicitement demandé par le brief. L'assistant 4 étapes (saison/championnats/destination/créer, Sprint 26) est entièrement retiré au profit d'une page unique réorganisée : (1) les championnats deviennent le point d'entrée, affichés immédiatement sous le titre, regroupés par catégorie (`categories.get_groups_for`, inchangé) dans des accordéons à un seul niveau (`ft.ExpansionTile`) — plus la longue liste de cases à cocher ; (2) chaque championnat est un bouton sélectionnable réutilisant le style `selected=True` de `theme.card()` (anticipé dans sa docstring depuis le Sprint 26/30 — "used for step indicators and chosen options" — mais jamais consommé jusqu'à ce sprint), sélection multiple conservée, jamais de boutons radio ; (3) le sélecteur de saison devient secondaire, déplacé dans un nouveau slot `trailing` de `PageHeader` (haut droite de l'en-tête) ; (4) le résumé de sélection (Sprint 40) devient réellement permanent (plus de conditionnement par étape) et affiche désormais aussi le nombre de championnats sélectionnés (`len(state.selected_championships)`, connu instantanément — affiché même pendant que le reste du résumé charge encore) ; (5) l'explorateur de saison (Sprint 41) ne s'affiche que si ≥ 1 championnat est sélectionné, `EmptyState` sinon — comportement déjà existant (`build_season_explorer([], ...) == ()`), simplement plus jamais masqué derrière une étape ; (6) "Créer mon calendrier" (+ le champ de destination) vit dans un nouveau slot `footer` de `PageContainer`, un pied de page fixe qui ne défile jamais avec le reste — la solution la plus simple retenue pour "toujours visible sans scroll", en réutilisant la même largeur/padding/alignement que `theme.page_shell`, juste une région scrollable (en-tête + corps) séparée du pied de page fixe. `PageHeader`/`PageContainer` : deux nouveaux paramètres optionnels (`trailing`, `footer`), tous deux `None` par défaut — les 5 autres pages de l'application restent structurellement et visuellement identiques, vérifié par `TestAllViewsShareTheSameGrid` intégralement vert après le changement. `gui/views/calendar.py` : nouvelles dataclasses `ChampionshipButtonData`/`ChampionshipCategoryData` (display-ready, mirroring le style déjà établi par `SeasonEventRow`/`ChampionshipCardData`) ; `main_view.py` résout `get_groups_for`/`get_display_name` et construit ces dataclasses (comme il construisait déjà les checkboxes avant ce sprint), la vue ne fait que les rendre — aucune logique métier dans la vue, y compris pour le nouvel état "quel accordéon est ouvert" (`expanded_categories: set[str]`, tracké dans `main_view.py`, initialisé une fois avec les catégories contenant une présélection pour que le choix par défaut soit visible sans clic). `GenerateState` (`gui/models.py`) perd `current_step`/`STEP_COUNT`/`step_valid`/`can_advance`/`can_go_back`, devenus sans objet — `is_ready()` et les autres champs inchangés. Retrait des 11 chaînes `wizard_*` devenues mortes. Tests : `TestGenerateStateWizard` (15 tests) retirée, ~30 tests de vue adaptés (nouvelle structure de contrôles), ~35 nouveaux tests (accordéons, boutons, footer sticky, trailing header, résumé permanent). **1650 tests total**.
44. **Favoris intelligents (Sprint 44)** — "Mes favoris" passe de placeholder à fonctionnalité centrale : les favoris deviennent une préférence globale utilisée automatiquement partout (Dashboard, Ce week-end, Mon calendrier). Nouveau `gui/favorites_service.py::FavoritesService` (classe, mirroring le style `ConfigService` — `config/service.py` — plutôt que les modules fonctionnels habituels du paquet `gui/`, car "Service" implique explicitement un objet avec état/méthodes) : `list()`/`is_favorite()`/`add()`/`remove()`/`toggle()`, persisté via `gui/preferences.py` (nouvelle clé `favorite_championships` dans `_DEFAULTS`) — pas un second fichier de configuration, conforme à "la persistance doit être centralisée". Extraction `gui/components/championship_selector.py` (`ChampionshipButtonData`/`ChampionshipCategoryData`/`build_championship_selector`) depuis `gui/views/calendar.py` (Sprint 43) une fois "Mes favoris" devenu un second consommateur réel du même besoin (accordéon de championnats sélectionnables) — même principe de mutualisation-au-second-usage que `_fetch_weekend_entries` (Sprint 39) ou `session_type_label` (Sprint 42) ; `gui/views/calendar.py` et le nouveau `gui/views/favorites.py` importent tous deux ce composant partagé, aucune duplication du code de sélection. "Mes favoris" (page réelle) : mêmes accordéons/boutons que "Mon calendrier", "sélectionné" signifiant "favori" plutôt que "choisi pour cette génération" — le composant lui-même est indifférent à la distinction. Tri "favoris en premier" : `upcoming_weekend.find_upcoming_weekend`/`_group_entries_for_display` gagnent un paramètre optionnel `favorite_ids: frozenset[str]` (tri stable, favoris devant, ordre catégorie/chronologique préservé entre eux) — `gui/dashboard.py::build_dashboard_data` le transmet tel quel à `find_upcoming_weekend`, donc Dashboard et Ce week-end partagent la même et unique implémentation du tri (jamais deux). `controller.get_upcoming_weekend`/`get_dashboard_data` construisent `FavoritesService()` en interne (comme `ConfigService()` l'est déjà dans `_fetch_weekend_entries`) et lui passent `favorite_ids`, sans changer leur signature d'appel côté `main_view.py`. "Mon calendrier" : `state.selected_championships` est initialisé depuis `favorites_service.list()` s'il n'est pas vide, sinon repli sur l'ancien comportement (`prefs.get("selected_championships", DEFAULT_SELECTED)`) — un seed initial, pas une synchronisation continue en cours de session. Bug latent corrigé en cours de route : `main_view.py::_save_prefs()` écrivait un dictionnaire littéral neuf à chaque sauvegarde, effaçant silencieusement toute clé non listée (dont `favorite_championships`, ajoutée par ce sprint) — corrigé en lecture-fusion-écriture (`load_preferences()` frais, ne remplacer que les clés possédées, `save_preferences()` du dict entier), documenté comme convention obligatoire dans le docstring de `preferences.py`. Basculer un favori déclenche un nouveau `_load_weekend()`/`_load_dashboard()` (mêmes fonctions que le chargement initial, cache HTTP existant absorbant le coût réseau) plutôt qu'un tri local des données déjà récupérées — garantit qu'il n'existe qu'une seule implémentation du tri favoris, jamais une variante "live" divergente. Risque de non-déterminisme détecté et corrigé avant qu'il ne cause un test instable : un vrai fichier de préférences (`~/.config/motorsport-calendar/gui_prefs.json`) existait déjà sur la machine de développement (issu des sessions précédentes) — nouvelle fixture `autouse` dans `tests/conftest.py` isolant `preferences._PREFS_FILE` vers un répertoire temporaire pour toute la suite. 36 nouveaux tests. **1686 tests total**.
45. **Recherche globale (Sprint 45)** — cinquième sprint "valeur" du projet (après Dashboard/Mon calendrier/explorateur de saison/fiche événement) : aucun nouveau provider, aucune nouvelle source de données, aucun appel réseau supplémentaire — une recherche instantanée exploitant uniquement les données déjà chargées (championnats enregistrés + `year_events`, le même dict que "Mon calendrier" utilise depuis le Sprint 40). Nouveau `gui/search_service.py::SearchService` (classe, mirroring le style `FavoritesService`/`ConfigService` — "Service" implique un objet avec état/méthodes, ici l'index) : `build_index(championship_ids, year_events)` construit un index immuable une seule fois par changement de données, `search(query)` est purement O(taille de l'index), jamais O(un nouveau parcours des providers) — satisfait littéralement "ne jamais reparcourir l'ensemble des providers à chaque frappe". Normalisation "compacte" (`unicodedata.normalize("NFKD", ...)` + suppression des marques combinantes + `casefold()` + ne garder que les caractères alphanumériques) choisie après vérification live contre les exemples explicites du brief : une normalisation plus légère (accents/casse seulement, séparateurs préservés) aurait échoué sur "Le Mans" vs "lemans" et "spa francorchamps" vs "Spa-Francorchamps" — la forme compacte les fait tous correspondre, au prix assumé de ne plus distinguer les frontières de mots (jeu de données petit et curé, 17 championnats, pas un moteur de recherche généraliste). Résultats groupés par type (`SearchResults.championships/events/circuits`, tuples déjà triés) : pertinence d'abord (0 = correspondance exacte, 1 = préfixe, 2 = sous-chaîne ailleurs), puis alphabétique (`title.casefold()`) en cas d'égalité. Réutilise entièrement les modèles existants plutôt que de dupliquer une normalisation : `display_names.get_display_name` pour les noms de championnat, `event_display.normalize_event_display` (Sprint 32, ADR-023) pour les noms d'événement/circuit — un circuit ou un événement ne s'affiche jamais différemment en recherche qu'ailleurs dans l'app. Circuits dédupliqués par nom normalisé (le même circuit accueille plusieurs événements/années ; première occurrence conservée). Nouvelle page `gui/views/search.py::build_search_view(search_field, results, has_query)` — pure mise en page, aucune logique de recherche/tri : composition `PageHeader(icon=ft.Icons.SEARCH, subtitle="N résultats")` + `Section`/`SectionHeader`/`CardList` par groupe non vide + `EmptyState` (deux messages distincts : "commencez à taper" vs "aucun résultat", distingués par `has_query`) — cette composition exacte était déjà anticipée dans `test_gui_components_layout.py::TestLayoutSystemIntegration` depuis le Sprint 31 (icône `ft.Icons.SEARCH`, sous-titre "3 résultats"), confirmant le choix plutôt que d'en inventer un nouveau. Devient une nouvelle destination de navigation (entre "Mon calendrier" et "Mes favoris"), aucune position n'étant imposée par le brief. `main_view.py` : `search_service = SearchService()` construit une fois ; `build_index()` appelé à deux endroits — une fois au démarrage avec `year_events or {}` (les championnats sont cherchables immédiatement, avant même que le fetch en arrière-plan ne résolve) et une seconde fois dans `_load_year_events()` à chaque résolution d'année (les résultats ne restent jamais périmés après un changement d'année sur "Mon calendrier") ; champ de recherche (`ft.TextField(on_change=...)`) déclenchant `_refresh_search_view()` à chaque frappe — recherche instantanée, aucun debounce nécessaire (index en mémoire, coût négligeable). Limitation de périmètre assumée et documentée (pas un bug) : la recherche ne couvre que l'année actuellement parcourue sur "Mon calendrier" (`year_events`), pas l'historique complet toutes années confondues — cohérent avec "aucun appel réseau supplémentaire". 46 nouveaux tests (29 `SearchService` + 17 vue "Recherche"). **1732 tests total**.
46. **Moteur de notifications (Sprint 46)** — premier sprint de fondations pures depuis "Mon calendrier" (Sprint 43) : contrairement aux 5 sprints "valeur" précédents, aucune interface n'est construite ce sprint (brief explicite : "Aucune notification système... n'est attendue", pas de section "Interface" dans le brief lui-même) — uniquement un moteur de calcul, réutilisable plus tard par une couche spécifique Windows/Linux/macOS sans modification. Nouveau `gui/notification_service.py::NotificationService` (classe, mirroring `FavoritesService`/`SearchService` — un service qui possède un état construit depuis les préférences persistées) : `compute_notifications(year_events, *, now, lead_times=None, kinds=None, favorites_only=None, favorite_ids=frozenset())` — calcule toutes les notifications "à venir" (`trigger_at >= now`, jamais une notification déjà due) à partir de `year_events`, le même dict que `controller.get_calendar_year_events` produit déjà et que "Mon calendrier"/"Recherche" consomment (Sprint 40/45) — aucun nouvel appel réseau, satisfait littéralement "à partir des données déjà chargées". `now` est un paramètre requis, jamais lu sur l'horloge système à l'intérieur du service (même convention que `upcoming_weekend.find_upcoming_weekend`, Sprint 29) — calcul entièrement déterministe et testable. `NotificationKind` (StrEnum, 5 valeurs : `WEEKEND_START`/`FIRST_SESSION`/`QUALIFYING`/`SPRINT`/`RACE`) : `QUALIFYING`/`SPRINT`/`RACE` s'ancrent chacun sur les sessions de l'`Event` dont le `SessionType` correspond exactement (0 ou 1 session, jamais supposé exactement 1 — un événement sans sprint ne produit légitimement aucune notification Sprint) ; `WEEKEND_START`/`FIRST_SESSION` s'ancrent tous deux sur la session la plus précoce de l'événement (même instant) — décision assumée et documentée (ADR-037) : le modèle de domaine n'a pas de notion de "début de week-end" distincte de "première session" à ce jour, les deux kinds sont néanmoins gardés distincts (pas fusionnés en un seul) parce que le brief les liste séparément et qu'un futur sprint pourrait les distinguer (ex. un jour d'essais libres avant la première session chronométrée). Délais totalement configurables : `lead_times` accepte n'importe quelle combinaison de `timedelta` simultanément (une notification par combinaison session × délai encore à venir), pas une seule valeur — les 4 exemples du brief (24h/12h/1h/15min) fonctionnent tous ensemble en un seul appel. Nouveau paramètre `kinds` (non demandé explicitement par le brief mais cohérent avec "les délais doivent être configurables" et nécessaire pour des tests déterministes "une notification"/"plusieurs notifications" — un événement mono-session est structurellement toujours à la fois `WEEKEND_START`, `FIRST_SESSION` et son propre type spécifique, donc au moins 3 kinds simultanés sans un filtre) : `None` (défaut) calcule les 5 kinds, un futur sprint Préférences pourra en faire une option utilisateur au même titre que `lead_times`. `favorites_only`/`favorite_ids` : fonctionne sur tous les championnats par défaut, restreint aux favoris si demandé — `None` utilise la préférence persistée `self.favorites_only`, un `bool` explicite l'outrepasse pour un appel donné sans toucher la préférence (utile pour les tests). Réutilise entièrement les modèles existants : `display_names.get_display_name`/`event_display.normalize_event_display` (Sprint 32, ADR-023) — un événement ne s'affiche jamais différemment dans une notification qu'ailleurs dans l'app, aucune seconde normalisation inventée. 3 préférences persistées (`notifications_enabled`/`notifications_default_lead_time_minutes`/`notifications_favorites_only`) ajoutées à `_DEFAULTS` dans `gui/preferences.py` (même fichier centralisé que `favorite_championships`, Sprint 44) — `NotificationService.__init__` les lit, `set_enabled`/`set_default_lead_time`/`set_favorites_only` les persistent en lecture-fusion-écriture, même discipline que `FavoritesService._save()`. Aucune interface construite : pas de `gui/views/notifications.py`, aucune destination de navigation ajoutée, `main_view.py` non modifié — le service existe mais n'est câblé nulle part encore, conforme au brief ("construire les fondations"). 33 nouveaux tests (31 `NotificationService` + 2 préférences), couvrant explicitement chacun des 6 scénarios de validation du brief : aucune notification (index vide, événement sans session, notification déjà due), une notification (filtre `kinds` isolant un seul type), plusieurs notifications (délais multiples, événements multiples, tri du plus proche au plus lointain), favoris uniquement, changement de fuseau horaire (session construite avec un `ZoneInfo` non-UTC, ex. Asia/Tokyo, résultat identique à son équivalent UTC), changement de saison (même instance de service, deux appels avec des `year_events` différents, aucune fuite de l'ancienne saison — contrairement à `SearchService`, ce service n'a pas d'index persistant à reconstruire, chaque appel est un calcul frais). **1765 tests total**.
47. **Circuit Explorer (Sprint 47)** — les circuits deviennent des objets de premier niveau : jusqu'ici, un circuit n'était que du texte sur une `ChampionshipCard` (nom + pays, jamais interrogeable). Nouveau `gui/circuit_service.py::CircuitService` (classe, mirroring `SearchService` — "aucun état persistant au-delà du dernier index construit, reconstruit intégralement à la demande, aucun accès réseau") : `build_index(year_events)` agrège chaque circuit à travers tous les championnats/saisons déjà chargés en un `CircuitProfile` (`circuit_key`, `name`, `country`, `championship_ids`/`championship_names`, `championship_count`, `total_events`, `first_season`, `last_season`, `events: tuple[CircuitEventEntry, ...]`) ; `get_circuit(circuit_key)`/`list_circuits()` interrogent l'index construit. Réutilise entièrement les modèles existants plutôt que d'inventer une seconde normalisation : `display_names.get_display_name` pour les noms de championnat, et trois fonctions promues **publiques** dans `gui/event_display.py` (Sprint 32, ADR-023) — `normalize_key` (déplacée depuis l'ancien `search_service._normalize`, Sprint 45, une fois la déduplication de circuits devenue un second consommateur réel de la même normalisation "compacte" accents/casse/séparateurs — même principe de mutualisation-au-second-usage que `session_type_label`/`championship_selector`), `circuit_display_name` (nouvelle — le nom propre d'un circuit, indépendant de toute redondance avec un événement précis, contrairement à `_resolve_circuit_name`) et `resolve_country` (renommée depuis `_resolve_country`, même règle "jamais 'Unknown' affiché tel quel"). `search_service.py` a été adapté pour importer `normalize_key` au lieu de sa propre copie privée — comportement strictement inchangé, vérifié par ses 29 tests existants intégralement verts sans aucune modification. Déduplication cross-provider validée en direct sur données réelles : "Spa-Francorchamps" fusionne automatiquement 5 championnats (F1, ELMS, MLMC, GTWC Europe, IGTC) en une seule entité malgré des orthographes différentes selon le provider. Country "best-available-across-providers" : si un championnat renvoie "Unknown" pour un circuit qu'un autre championnat décrit avec un vrai pays, le vrai pays gagne et n'est plus jamais écrasé — jamais l'inverse. `EventDisplayData` (event_display.py) gagne un champ `circuit_key: str | None`, calculé dans `normalize_event_display()`, toujours en lockstep avec `circuit_name` (`None` exactement quand la ligne circuit est masquée pour cet événement précis — rien à cliquer si rien n'est affiché). `EventDetails` (event_details.py) transmet ce même `circuit_key` sans aucune logique propre. Fiche Circuit : depuis la fiche événement (Sprint 42), le nom du circuit devient cliquable et ouvre une nouvelle boîte de dialogue (`main_view.py::_show_circuit_details_dialog`, même patron exact que `_show_event_details_dialog`/`_show_success_dialog` — `ft.AlertDialog`/`page.show_dialog`/`page.pop_dialog`) affichant nom, pays, championnats (puces `theme.chip`, même patron que la section "Championnats ce week-end" du Dashboard), historique chronologique des événements, et nombre total de courses. `components/championship_card.py::build_championship_card` gagne un nouveau point d'extension optionnel `on_circuit_click: Callable[[], None] | None = None` — exact même contrat que `footer` (Sprint 30) : `None` partout ailleurs (Ce week-end, Dashboard, Favoris) laisse la ligne circuit strictement inchangée (texte simple, `theme.Colors.TEXT_MUTED`) ; seule la fiche événement l'active, la ligne circuit devenant alors un `ft.Container` cliquable (`theme.Colors.PRIMARY`, seul jeton sémantique déjà existant réutilisé, aucune nouvelle couleur). 35 nouveaux tests (15 `CircuitService` + 5 `ChampionshipCard` `on_circuit_click` + 2 `EventDetails.circuit_key` + 13 `event_display` `normalize_key`/`circuit_display_name`/`resolve_country`/`circuit_key`). **1800 tests total**.
48. **Finalisation des providers (Sprint 48)** — trois championnats restaient volontairement en mode "stub" (`OfficialWecSource`/`OfficialImsaSource`/`OfficialWorldSbkSource`, tous `raise NotImplementedError`) : objectif, remplacer les implémentations temporaires par des providers fonctionnels, API officielle en priorité, scraping en dernier recours. Recherche en direct (pas d'hypothèse reprise telle quelle d'un sprint précédent, chaque piste re-vérifiée) sur les trois : **WEC** — confirmé en direct que fiawec.com tourne sur exactement le même CMS/JSON-LD schema.org qu'ELMS/MLMC (piste déjà identifiée au Sprint 35, jamais vérifiée jusqu'ici) : chaque page course (`/en/race/{slug}`) embarque un bloc `SportsEvent`/`subEvent` identique dans sa structure. **IMSA** — ré-investigué : imsa.com bloque désormais jusqu'à son propre `robots.txt` (Cloudflare plus strict qu'au Sprint 36, blocage confirmé au niveau infrastructure, pas seulement anti-scraping), le portail Al Kamel (`imsa.results.alkamelcloud.com`) reste une archive de résultats post-course (dossiers de session qui n'existent qu'après qu'une session ait eu lieu), et le tableau "Schedule" de Wikipedia (vérifié via l'API MediaWiki) donne toujours seulement des dates de course, jamais d'heures de session — aucune source exploitable trouvée, stub inchangé. **WorldSBK** — ré-investigué : deux nouveaux hôtes de la famille Pulselive découverts (`api.wsbk.pulselive.com`, réel mais aucune route valide ; `wsbk.pulselive.com`, un simple miroir SPA du site, pas une API) en plus du candidat déjà connu (`wsbk-api-origin.gplat-test.pulselive.com`, toujours injoignable) — aucun endpoint événements exploitable sans automatisation navigateur, stub inchangé. Seul WEC est donc finalisé ce sprint — résultat honnête conforme à la consigne "rechercher la meilleure source", pas un échec de sprint. Implémentation WEC : `OfficialWecSource(AcoSportsEventSource, WecSource)`, conservée sous son nom historique et sa clé d'enregistrement `"official"` (renommer aurait cassé le défaut `ProvidersConfig.wec.source == "official"` — "ne jamais casser les providers existants" pris au pied de la lettre). `aco_series/sports_event_base.py` (partagé ELMS/MLMC) étendu de façon purement additive, jamais dupliqué : 3 nouveaux types de session dans `_LABEL_RULES` ("Free Practice 4" → `FREE_PRACTICE` générique, "Hyperpole" → `SessionType.HYPERPOLE` déjà présent dans le modèle de domaine, "Warm-up" → `TEST`, le type existant le plus proche — aucun de ces libellés n'apparaît jamais chez ELMS/MLMC, vérifié par leur suite de tests existante intégralement verte sans modification) ; `"prologue"` ajouté aux mots-clés de slugs exclus (le prologue pré-saison de WEC, jamais présent chez ELMS/MLMC) ; deux nouveaux points d'extension explicites créés pour accueillir les divergences WEC sans dupliquer le pipeline HTTP/cache/parsing partagé — `_race_session_end(first_start, event_end, event_name)` (comportement par défaut inchangé pour ELMS/MLMC : faire confiance à l'`endDate` JSON-LD si plausible) et `_race_url_belongs_to_season(url, year)` (`True` inconditionnel par défaut pour ELMS/MLMC). Bug réel détecté et corrigé en conditions live (même esprit que les investigations Sprint 32/35/37/38) : l'`endDate` JSON-LD de fiawec.com ne reflète JAMAIS la durée réelle d'une course WEC (toujours minuit du dernier jour annoncé) — pour la plupart des courses ceci échoue la vérification de plausibilité existante et se rabat correctement sur la durée par défaut, mais pour les 24 Heures du Mans le delta (~8h) passe silencieusement le test de plausibilité et aurait produit une session Course de 8h au lieu de 24h. `OfficialWecSource._race_session_end` surcharge donc entièrement cette logique : durée déduite du nom de l'épreuve (`"X Hours of Y"`, motif présent sur 6 des 8 manches 2026, recherché par regex) avec une table explicite pour les 2 exceptions nommées différemment ("Lone Star Le Mans" = 6h, "Qatar 1812km" = 10h, durées confirmées via fiawec.com/Wikipedia, jamais devinées) et un repli générique à 6h (format le plus courant du calendrier WEC) pour un nom futur non reconnu. `OfficialWecSource._race_url_belongs_to_season` corrige un second piège découvert en direct : la page `/en/season/{year}` de fiawec.com liste à la fois l'année demandée ET la suivante dans le même DOM (contrairement à ELMS/MLMC) — filtré par le suffixe `-{year}` de l'URL. Nouveau `wec/circuit_data.py` : pays résolu **dynamiquement** depuis le champ `location.address` du JSON-LD (`"{ville}, {code ISO 3166-1 alpha-3}"`, ex. "Imola, ITA") plutôt qu'une table statique par circuit — même raisonnement "préférer la donnée en direct" déjà justifié pour `sro_series/circuit_data.py` (Sprint 37) ; la table statique ne sert que de repli pour une adresse absente/non reconnue. Validation complète sur la saison 2026 réelle : 8 événements, 50 sessions, UID uniques, fuseaux horaires corrects sur les 8 circuits (dont la fusion automatique confirmée de "Spa-Francorchamps" avec 4 autres championnats via `CircuitService`, Sprint 47) — intégration vérifiée en direct dans le Dashboard, "Ce week-end", "Recherche", et l'agrégateur CLI (`generate`/`generate-wec`). Adaptation de tests existants qui s'appuyaient implicitement sur l'échec naturel de WEC (`NotImplementedError` non mocké) — `test_gui_controller.py` (WEC ajoutée à `_WEEKEND_SOURCE_PATHS`, désormais mockée comme toute autre source réelle ; `TestGenerateCalendarWec` renommée `TestGenerateCalendarImsa`, IMSA jouant désormais ce rôle de "source non implémentée" de référence) et `test_cli_generate.py` (nouvelle entrée `OfficialWecSource` dans la fixture autouse `_isolate_support_series`, faisant échouer WEC par défaut sauf mock explicite — restaure le comportement exact de tous les tests existants sans modifier leur corps). 37 nouveaux tests (10 `test_aco_sports_event_base.py` + 30 `test_wec_provider.py`, dont -1 test stub retiré ; -2 dans `test_cli_generate_wec.py`, tests devenus obsolètes retirés ; adaptations sans tests nets nouveaux dans `test_cli_generate.py`/`test_gui_controller.py`). **1837 tests total**.

49. **Packaging Alpha (Sprint 49)** — sprint explicitement non-fonctionnel : "aucun nouveau provider, aucune nouvelle page, aucune évolution métier, aucun changement de Design System" — objectif unique, rendre le projet distribuable (Linux + Windows) sans rien changer au comportement. Trois bugs de packaging réels identifiés et corrigés, tous de la même famille — un chemin par défaut soit codé en dur pour Linux/macOS, soit relatif au CWD du process plutôt qu'à un répertoire utilisateur stable : (1) `cache/http_cache.py::HttpCache.__init__` par défaut `Path(".cache")` — écrivait silencieusement dans le dépôt Git en dev, et de façon imprévisible une fois packagé selon le CWD de lancement ; (2) `gui/preferences.py::_PREFS_FILE` codé en dur `~/.config/motorsport-calendar/` — non fonctionnel sur l'idiome Windows (`%APPDATA%`) bien que non cassé ; (3) `config/service.py::ConfigService._DEFAULT_PATHS` même limitation pour `config.yaml` niveau utilisateur. Nouveau `utils/paths.py` (`user_config_dir`/`user_cache_dir`) : convention XDG Base Directory sous Linux (`$XDG_CONFIG_HOME`/`$XDG_CACHE_HOME`, repli `~/.config`/`~/.cache`) et convention Windows (`%APPDATA%`/`%LOCALAPPDATA%`) — même patron `sys.platform == "win32"` déjà utilisé ailleurs dans la GUI (`main_view.py::_open_folder`, `views/about.py`), pas de nouvelle dépendance (`platformdirs` écarté). `Path("")` est *truthy* en Python (`== Path(".")`) — piège évité en testant la vérité de la variable d'environnement brute avant de l'envelopper dans `Path(...)`. Quatrième bug, spécifique à Flet : `gui/app.py`'s `assets_dir` passé à `ft.run()` se résout relativement au **CWD au lancement**, pas au fichier appelant (confirmé en lisant le source de `flet/app.py`) — corrigé via `_ASSETS_DIR = str(Path(__file__).parent / "assets")`, un chemin absolu **calculé à l'exécution**, jamais littéral (l'interdiction "aucun chemin absolu" du brief vise les chemins codés en dur, pas ce patron portable standard). Assets officiels (`BApps-Studio/.../Branding/`) copiés octet-identiques dans `gui/assets/` : `favicon-16.png`, `favicon-32.png`, `icon_windows.ico` (convention Flet pour l'icône Windows multi-résolution, `icon.png` restant l'icône de repli toutes plateformes, déjà présente et confirmée identique par comparaison pixel), `logo/mc-icon.svg`, `logo/logo-horizontal.svg`, `logo/logo-vertical.svg`. `page.window.icon = "icon.png"` câblé dans `main_view.py` (chrome de fenêtre OS, jugé hors du périmètre "vues"/"Design System" du brief). Décision de périmètre explicite : les 6 fichiers sont bien "intégrés au build" (présents, adressables via `assets_dir`) sans que `theme.logo_placeholder()` soit remplacé dans les vues — remplacer des placeholders visuels par de vraies images est un chantier de Design System, explicitement hors sprint (documenté dans `gui/assets/logo/README.md`, réservé pour un futur sprint). Export ICS (`exporters/ics.py`) vérifié déjà propre — aucune modification nécessaire, `output_path` entièrement fourni par l'appelant sans repli ni référence au dépôt, désormais couvert par des tests explicites (`TestExportIsPackagingSafe`). Build réel tenté en direct sur cette machine (pas de simulation) : `flet build linux motorsport_calendar/gui --module-name app` — deux corrections de la commande elle-même découvertes en cours de route (`python_app_path` doit pointer sur le dossier contenant `app.py`, pas la racine du dépôt ; `--module-name app` requis car l'entrée n'est pas `main.py`) ; le SDK Flutter (3.41.7) s'installe seul au premier lancement (~1-2 Go) ; le build atteint l'étape de compilation native ("Building Linux application…") confirmant que la configuration du packaging elle-même (résolution du module, bundling des assets, `pyproject.toml`) est correcte de bout en bout — seul blocage rencontré : outillage système manquant sur cette machine (`clang`, `cmake`, `ninja-build`, `pkg-config`, et après vérification de la documentation officielle Flet, également `binutils`, `llvm`, `lld` — critique, absent de ma première liste — et `libunwind-dev`), un gap machine, pas un bug projet ; voir `docs/JOURNAL.md` pour l'issue exacte de cette installation. Build Windows non exécutable dans cet environnement (aucune machine Windows disponible, et Flet ne permet pas la cross-compilation Windows depuis Linux/macOS) — procédure et prérequis (Visual Studio 2022/2026 + workload "Desktop development with C++", Developer Mode pour les symlinks) transcrits fidèlement depuis la documentation officielle Flet dans le nouveau `docs/PACKAGING.md`, avec mention honnête "non exécuté ce sprint". 26 nouveaux tests (`test_utils_paths.py` complet + classes ajoutées dans `test_http_cache.py`, `test_config_service.py`, `test_gui_preferences.py`, `test_ics_exporter.py`, `test_packaging.py`) — piège évité deux fois : ni `HttpCache()` ni la lecture de `preferences._PREFS_FILE` en direct ne peuvent servir de test (le premier créerait un vrai répertoire sur la machine dev, le second est toujours écrasé par le fixture autouse `_isolated_gui_prefs`) — testés respectivement via `inspect.signature(...).parameters[...].default` et `inspect.getsource(...)` sans jamais instancier/toucher le vrai système de fichiers. **1863 tests total**.

50. **Audit & Consolidation (Sprint 50)** — sprint explicitement non-fonctionnel, le premier du genre après 49 sprints sans pause de consolidation : "aucune nouvelle fonctionnalité, aucun nouveau provider, aucune nouvelle page, aucun changement fonctionnel". Objectif : audit complet du projet, réduction maximale de la dette technique, sans changer le comportement utilisateur. Baseline établie avant toute modification (méthode : `git stash` temporaire vers le dernier commit réel pour distinguer la dette héritée des sprints précédemment non commités de la dette pré-existante) — 149 erreurs Ruff, 87 erreurs mypy sur `motorsport_calendar/` (hors `gui/build/`, artefacts Flet Sprint 49 jamais exclus jusqu'ici), 402 erreurs mypy sur `tests/`, 0 TODO/FIXME/XXX dans le code source. **Ruff : 149 → 0** — la moitié corrigée mécaniquement (`ruff check --fix`, imports/noqa/syntaxe dépréciée), le reste revu un par un : 4 `pytest.raises(Exception)` sur des mutations de modèles frozen narrowés en `pydantic.ValidationError` (vérifié en direct que c'est bien l'exception réellement levée, jamais supposé) ; 6 `lambda` assignées converties en `def` ; 5 `raise typer.Exit(...)` dans `cli.py` gagnent un `from exc` pour préserver la chaîne d'exception ; `gui/categories.py::Category(str, Enum)` aligné sur `enum.StrEnum` (déjà la convention de `SessionType`/`EventStatus`/`ChampionshipCategory`, vérifié avant de changer) ; `core/datasource/base.py::DataSource` (ABC marqueur sans méthode abstraite, délibéré) reçoit un `noqa` documenté plutôt qu'une fausse méthode abstraite inventée. **mypy `motorsport_calendar/` : 87 → 23** — signatures `dict`/`list` nues remplacées par des génériques précis dans `cache/http_cache.py`, `cli.py`, `config/`, `core/datasource/`, providers Formula 1 (openf1/jolpica)/Formula 2/support-series/MotoGP-series, `gui/preferences.py`/`strings.py`/`controller.py` ; un cas non-mécanique dans `jolpica.py::_get_json` (union `list | dict` mal réconciliée après le typage de `HttpCache.get_json`, résolu par un `cast()` documentant que l'API Jolpica ne renvoie jamais une liste à cet endpoint) ; deux packages de stubs manquants ajoutés aux dépendances dev (`types-PyYAML`, `types-icalendar`) plutôt que des suppressions locales. **Bug réel détecté et corrigé** : `core/service.py::CalendarService.export_championship` (classe orchestratrice exportée mais jamais appelée par le CLI ni la GUI, confirmé par recherche exhaustive — coverage à 0 % sur son corps) passait un `Championship` entier à `Exporter.export()` qui attend une liste d'`Event` ; corrigée pour récupérer réellement les événements via `provider.fetch_events` avant export — zéro impact utilisateur (jamais exécutée en pratique) mais la classe redevient correcte pour une future exposition programmatique. **mypy `tests/` : 402 → 157** — le `[mypy-tests.*]` de `mypy.ini` ne relâchait que `disallow_untyped_defs`/`disallow_any_generics`, laissant actifs `disallow_untyped_calls`/`check_untyped_defs`/`warn_return_any`, qui généraient des centaines de faux positifs sur des appels à `unittest.mock.AsyncMock` (jamais une vraie erreur de logique de test, les 1863 tests passaient déjà tous) — relâchement supplémentaire scopé aux seuls tests, zéro impact sur `motorsport_calendar/` (toujours `strict = True` intégral), fait tomber le compte à 210 sans toucher un fichier ; complété par la suppression mécanique de 24 `# type: ignore` devenus inutiles (vérifiés un par un via mypy lui-même, jamais supposés) et par la factorisation de `test_aco_sports_event_base.py::TestSessionTypeForLabel` (14 occurrences du même pattern `_session_type_for_label(...)[0]` non-narrowed → un helper `_type_for()` avec un seul `assert result is not None`, double bénéfice dette-mypy et duplication). **Dette restante (documentée, non traitée)** : les 23 + une bonne part des 157 erreurs restantes forment une seule famille déjà connue depuis le Sprint 26 — décalage entre les stubs de types Flet 0.80 (utilisés à l'écriture du code) et Flet 0.85.3 (réellement installé), sur les signatures `on_click`/`on_change` et certains attributs de `Control` ; non corrigée car la corriger demanderait de réécrire des signatures de callback dans `main_view.py` avec un risque réel de régression comportementale pour un gain de vérification statique seulement — hors périmètre "aucun changement fonctionnel" de ce sprint. **Code mort audité** (`vulture`, nouvellement installé) : quasiment rien à 80 % de confiance (1 faux positif, un paramètre Typer lu par introspection) ; le seul candidat réel, `gui/theme.py::BAppsColors` (palette jamais consommée par aucun token), est une classe de documentation-as-code assumée (traçabilité de la provenance du Brand Set), signalée dans `docs/AUDIT.md` mais non supprimée. **Docstrings** : 367 fonctions/classes publiques scannées par AST, 103 signalées manquantes au premier passage, triées via lecture des ABC correspondantes — 76 sont des redéfinitions triviales d'une méthode déjà pleinement documentée sur `Provider`/`Formula1Source`/etc. (convention Python standard, jamais dupliquées), les 21 vrais manques comblés (`config/service.py`, `gui/favorites_service.py`, `gui/notification_service.py`, `gui/search_service.py`, `gui/theme.py::Spacing/Radius/IconSize/FontSize`). **Architecture vérifiée, non modifiée** : aucun import circulaire sur les 136 modules du paquet (détection par parcours de graphe AST) ; 9 dépendances runtime déclarées, toutes réellement consommées (y compris `lxml`/`tzdata`, invisibles car jamais `import`ées directement) ; 9 tables `_CIRCUIT_DATA` dupliquées entre providers examinées et volontairement non fusionnées (espaces de clés différents par source de données, fusion = couplage artificiel pour un gain cosmétique). **Tests** : duplication factorisée — un helper `_load()` identique copié-collé dans 8 fichiers (`test_aco_sports_event_base.py`, `test_sro_timetable_base.py`, 6 `test_cli_generate_*.py` pour les séries ACO/SRO) devient `tests/conftest.py::load_real_fixture()`, plain function (pas un `@pytest.fixture`, car appelée à l'import du module par certains fichiers, avant qu'un fixture pytest ne soit disponible). **Performance — optimisation mesurée, pas seulement affirmée** : `cli.py::generate` et `gui/controller.py::generate_calendar` attendaient chaque provider séquentiellement (`for`/`await`) alors que chacun interroge une API distante indépendante — benchmark synthétique confirmant ~10x sur 10 providers à latence égale, remplacé par `asyncio.gather` (préserve l'ordre des résultats, donc fichier ICS et résumés strictement identiques). Deux nouveaux tests de non-régression mesurent le *spread* des timestamps de démarrage de chaque appel provider mocké (plus robuste qu'un budget de temps total, qui serait pollué par le coût fixe de `registry.discover()`) et ont été **vérifiés pour échouer contre l'ancienne implémentation séquentielle** avant validation contre la nouvelle — pas des tests qui passeraient de toute façon. Nouveau `docs/AUDIT.md` : rapport complet (état général, points forts/faibles, dette restante, 10 fichiers les plus volumineux — `main_view.py::build_main_view` à 771 lignes identifiée comme seule vraie anomalie de taille du projet, non découpée ce sprint par prudence — services les plus critiques, recommandations). 2 nouveaux tests. **1865 tests total**.

51. **Vérification des mises à jour (Sprint 51)** — objectif : informer l'utilisateur qu'une nouvelle version existe, sans jamais rien télécharger/installer/redémarrer automatiquement, sans coupler la logique à GitHub ni à aucune plateforme. Nouveau `gui/update_service.py::UpdateService` — même contrat "utilisable seul, aucun `import flet`" que `FavoritesService`/`NotificationService`/`SearchService`, vérifié par un test dédié (`inspect.getsource`, cherche `"import flet"` dans le source du module). `manifest_url` et `current_version` sont entièrement fournis par l'appelant (jamais lus en interne, même discipline "aucun état global caché" que `NotificationService.compute_notifications`'s `now` explicite) — le module lui-même n'a aucune connaissance de GitHub ni d'aucun hébergeur particulier. `is_newer(current, candidate)` (module-level, pure) : comparaison **numérique** composant par composant (`parse_version` : `"0.4.10"` → `(0, 4, 10)`), jamais lexicographique — le piège explicitement nommé par le brief (`"0.4.9" > "0.4.10"` en comparaison de chaînes, `'9' > '1'` au premier caractère différent) vérifié en direct dans un test (`assert "0.4.9" > "0.4.10"` comme sanity-check du piège, puis `is_newer("0.4.9", "0.4.10") is True` comme preuve du correctif). Longueurs différentes complétées par des zéros avant comparaison (`"0.5"` et `"0.5.0"` comparent égales, pas l'inverse — un piège plus subtil que le premier, découvert en réfléchissant à la comparaison de tuples Python : `(0, 5) < (0, 5, 0)` nativement, ce qui aurait rendu `"0.5.0"` faussement "plus récente" que `"0.5"`). `UpdateService.check_for_update()` ne lève **jamais** d'exception — réseau absent (`httpx.ConnectError`), timeout, erreur HTTP, JSON invalide, manifeste incomplet (champs `version`/`release_date`/`title`/`summary`/`url` requis, `mandatory` optionnel défaut `False`), version illisible : tout est capturé et renvoyé via `UpdateCheckResult.error`, jamais raise — une vérification échouée ne doit jamais faire planter le démarrage de l'app. Nouveau `config/models.py::UpdateConfig` (`config.update.manifest_url`, vide par défaut) : c'est le SEUL endroit où une vraie URL est configurée, via `config.yaml` — aucune URL, aucun host, codé en dur nulle part dans le code (satisfait "ne pas coupler la logique à GitHub" et "le manifeste doit pouvoir provenir de n'importe quelle URL" simultanément). Nouvelle clé `gui/preferences.py::update_check_enabled` (défaut `True`, opt-out) — "prévoir la possibilité d'ignorer la vérification (préférence future)" du brief : la préférence existe et est lue, mais **aucune UI** ne l'expose encore ce sprint, même statut "fondations, pas d'interface" que `notifications_*` au Sprint 46 (précédent direct pour ce genre de choix de périmètre). `gui/controller.py::check_for_update()` (nouveau) résout ces deux entrées (config + préférence) puis délègue tout le reste à `UpdateService` — même rôle de "façade réseau de la GUI, aucune logique métier propre" que `generate_calendar`/`get_dashboard_data`, cohérent avec le principe explicite du brief "toute la logique doit rester dans le service, la vue ne fait qu'afficher le résultat". Câblage GUI dans `main_view.py` : `_check_for_update()` en tâche de fond au démarrage (`page.update_check_task`, même patron exact que `page.weekend_load_task`/`page.dashboard_load_task`), `_show_update_dialog()` affiche version actuelle/nouvelle version/résumé/bouton "Voir la version" (ouvre l'URL via `url_launcher`, même fallback `subprocess.Popen` Windows que `views/about.py::on_github_click` si `url_launcher.launch_url` échoue) — aucun bouton de téléchargement, aucune action automatique, uniquement une ouverture de navigateur sur clic explicite. Champ `mandatory` du manifeste surfacé comme un simple badge textuel dans la boîte de dialogue (jamais un blocage/une désactivation du bouton Fermer — inventer un tel comportement aurait été une forme d'"installation forcée" non demandée par le brief). 58 nouveaux tests (`test_update_service.py` complet — `parse_version`/`is_newer`/`_parse_manifest`/`UpdateService.check_for_update` via `httpx.MockTransport`, jamais de vrai appel réseau — + ajouts dans `test_config_service.py`/`test_gui_preferences.py`/`test_gui_controller.py`), couvrant explicitement chaque scénario nommé par le brief : même version, version plus récente (patch/mineure/majeure), version courante en avance sur le manifeste (cas non nommé mais symétrique, un build de dev ne doit jamais se voir proposer une "mise à jour" vers une version plus ancienne), manifeste invalide (pas un objet JSON, champs manquants), absence de réseau (`httpx.ConnectError`), timeout, erreurs HTTP 404/500, JSON malformé. **1923 tests total**.

52. **Préférences avancées (Sprint 52)** — objectif : transformer la page Préférences (un placeholder statique depuis le Sprint 26, jamais relié à rien de réel) en véritable centre de configuration, en réutilisant exclusivement les services déjà construits par les sprints précédents — `NotificationService` (Sprint 46), `FavoritesService` (Sprint 44), la préférence `update_check_enabled` (Sprint 51) — sans inventer de nouvelle logique métier dans la vue, et sans la moindre évolution du Design System/Layout System. Quatre sections : **Notifications** (activées/désactivées, favoris uniquement, délai par défaut — les 3 réglages déjà exposés par `NotificationService` depuis le Sprint 46 mais jamais câblés à une UI jusqu'ici), **Mises à jour** (activer/désactiver la vérification au démarrage — la préférence `update_check_enabled` existait déjà depuis le Sprint 51, explicitement documentée à l'époque comme "fondations, pas d'interface" ; ce sprint lui donne enfin son interface), **Calendrier** (année par défaut, rappel avant export), **Application** (Thème/Langue/Format horaire — préparés, non implémentés, conformément au brief). **Architecture — `PreferencesViewControls`, jamais `PreferencesModel` directement** : suit le patron déjà établi par `CalendarViewControls` (Sprint 43) plutôt que le patron valeur+callbacks de `build_favorites_view` — main_view.py construit chaque `ft.Switch`/`ft.Dropdown` avec son `on_change`/`on_select` déjà câblé (appelant `NotificationService.set_*`, ou lisant/écrivant directement `load_preferences()`/`save_preferences()` pour les 3 clés qui n'ont pas de service dédié), et `views/preferences.py` ne fait plus que les disposer dans les Sections existantes — zéro logique métier dans la vue, conforme à la lettre du brief. Découverte en cours de route : `ft.Dropdown` de cette version de Flet n'a pas de callback `on_change` mais `on_select` (contrairement à `ft.Switch`, qui a bien `on_change`) — vérifié par introspection directe (`inspect.signature`) avant d'écrire le code, plutôt que supposé par analogie avec `year_dropdown` (qui utilisait déjà `on_select`, un indice qui aurait dû suffire mais a été vérifié quand même). **`gui/preferences.py`** gagne deux clés : `default_year` (défaut `"current"`, une sentinelle plutôt qu'une année codée en dur — une année littérale figée au moment de l'écriture du code serait devenue fausse dès l'année suivante) et `ics_alarm_minutes` (défaut `30`, identique à `config.ics.alarm_minutes` pour un comportement inchangé tant que l'utilisateur ne touche pas la page). **Nouveau `gui/models.py::resolve_default_year()`** (pure, sans Flet) décode `default_year` en année réelle — retombe sur l'année courante pour toute valeur corrompue ou hand-editée, ne plante jamais le démarrage de "Mon calendrier" pour ça. **`gui/controller.py::generate_calendar()`** modifié : l'export ICS lit désormais `ics_alarm_minutes` depuis les préférences (repli sur `config.ics.alarm_minutes` si jamais enregistrée) — seul changement de comportement réel du sprint, scopé aux exports GUI uniquement (`cli.py` continue de ne lire que `config.yaml`, intentionnellement inchangé). **`gui/models.py::PreferencesModel` repensé** : les 6 champs hérités depuis les tout premiers sprints (`timezone`/`first_day_of_week`/`favorite_championships`/`preferred_calendar`/`bapps_sync_enabled`), tous décoratifs et documentés comme tels depuis le Sprint 44 (`favorite_championships` en particulier totalement supplanté par `FavoritesService`), retirés au profit des 3 seuls champs que le brief Sprint 52 nomme pour la section "Application" (`theme`/`language`/`time_format`) — repurposé plutôt que supprimé : "ces préférences doivent être pensées pour évoluer" (brief) justifie de garder un modèle typé prêt, même inerte. Tests : `test_gui_preferences_model.py` intégralement réécrit (mêmes classes, nouveaux champs), `test_gui_views.py::TestPreferencesView` intégralement réécrit avec un compte de cartes bordées recalculé (6 lignes de contrôle réel = 1 carte chacune, 3 lignes "coming soon" Application = 2 cartes chacune avec leur puce), deux nouveaux helpers de test mutualisés (`_flatten_controls`, `_collect_all_text` — ce dernier remplace une fonction locale dupliquée dans `TestAboutView`, un ménage mineur fait en passant), nouveaux tests pour `resolve_default_year` (7 cas, y compris valeur corrompue) et pour l'override `ics_alarm_minutes` de `generate_calendar` (défaut/override/désactivation totale, vérifiés sur le contenu réel du fichier ICS produit — `TRIGGER:-PT15M`/absence de `BEGIN:VALARM`). Aucune fonctionnalité de génération de calendrier, aucun provider, aucune page nouvelle. **1932 tests total**.

53. **Nouveautés & Centre d'accueil (Sprint 53)** — objectif : transformer le Dashboard (déjà la page d'accueil de facto depuis le Sprint 39) en véritable point d'entrée du produit, en réutilisant exclusivement `Dashboard`/`UpdateService`/`FavoritesService`/`ProviderRegistry` déjà construits — aucun nouveau service, aucun nouveau provider, aucune évolution des modèles métier. Trois nouvelles sections ajoutées à `gui/views/dashboard.py`, sans logique métier dans la vue (le brief est explicite sur ce point, répété dans chacune des trois consignes). **"Nouveautés"** : seule section du Dashboard qui peut s'omettre entièrement — `_news_section()` renvoie `None` (pas même un en-tête) si `data.update` est `None`, `update_available` est `False`, ou `manifest` est `None` (garde défensive : `UpdateService` couple toujours les deux en pratique, mais la vue ne suppose jamais cet invariant côté appelant) ; sinon une carte discrète (nouvelle version, résumé, bouton "Voir la version"). Le bouton ne réimplémente rien : `main_view.py::_show_update_dialog` avait déjà, depuis le Sprint 51, sa propre fermeture inline pour ouvrir l'URL de release (`url_launcher` + repli `subprocess.Popen`) — cette fermeture est extraite en fabrique nommée `_make_release_opener(url) -> Callable[[ft.ControlEvent], Awaitable[None]]`, utilisée à la fois par la boîte de dialogue de démarrage et par cette nouvelle carte, aucune seconde implémentation de "ouvrir une URL de release". **"Accès rapides"** : 4 cartes de navigation (Ce week-end/Mon calendrier/Recherche/Favoris), navigation pure — chaque carte appelle `on_navigate(key)` avec une clé string (`"weekend"`/`"calendar"`/`"search"`/`"favorites"`), jamais un index `NavigationRail` en dur ; `main_view.py` seul possède la correspondance clé → index (`_quick_access_nav_index`) et la fonction `_navigate_to(key)` qui met à jour `nav_rail.selected_index` + `content_area.content` — la vue ne connaît ni l'un ni l'autre, exactement la même séparation vue/navigation que le reste de l'app. **"État de Motorsport Calendar"** : réutilise `_stat_card()` (Sprint 39) tel quel pour 4 nouvelles statistiques — version actuelle (`motorsport_calendar.__version__`), championnats actifs (`registry.enabled(config.providers)`), fournisseurs réellement fonctionnels, favoris (`len(FavoritesService().list())`) — toutes résolues par `controller.get_dashboard_data()`, jamais codées en dur dans la vue. Le champ le plus délicat, "fournisseurs réellement fonctionnels" : pas de nouvelle métadonnée de capacité provider inventée, calculé dans `gui/dashboard.py::build_dashboard_data()` (le seul endroit du sprint avec une vraie logique, volontairement dans le module "compute" pas la vue) comme le nombre de `championship_id` distincts ayant produit au moins une entrée parmi celles déjà récupérées pour les 2 années — un stub qui échoue systématiquement (IMSA/WorldSBK) n'y contribue jamais, tandis qu'il compte bien dans `active_championships` (activé dans `config.yaml`) ; l'écart honnête entre les deux nombres est précisément ce que cette statistique doit montrer, pas un défaut à corriger. `DashboardData` gagne 5 champs passthrough (`active_championships`, `favorite_count`, `current_version`, `update`) résolus par `controller.get_dashboard_data()`, qui récupère désormais les entrées week-end et la vérification de mise à jour **en concurrence** (`asyncio.gather`, même patron que Sprint 50 pour les fetches provider) plutôt que séquentiellement. Tests : `TestBuildDashboardDataStatusFields`/`TestBuildDashboardDataFunctionalProviders` (12 tests, `test_gui_dashboard.py`) pour la couche "compute", `TestDashboardViewNews`/`TestDashboardViewQuickAccess`/`TestDashboardViewStatus` (17 tests, `test_gui_views.py`) pour la couche vue — couvrant explicitement les 4 scénarios nommés par le brief ("Dashboard sans nouveauté", "Dashboard avec nouveauté", "accès rapides", "informations d'état") ; `TestDashboardView._make_data` promu en fonction module-level `_make_dashboard_data()` pour être réutilisable par les 3 nouvelles classes, 3 assertions de comptage de cartes bordées préexistantes recalculées (6 → 14, +8 pour les 2 nouvelles sections toujours présentes). Aucune nouvelle fonctionnalité métier, aucun nouveau provider, aucune nouvelle dépendance, aucune duplication de logique. **1961 tests total**.

54. **Préparation Beta — recette UX (Sprint 54)** — objectif : le premier sprint de recette pure depuis l'Audit & Consolidation (Sprint 50, dette technique) — celui-ci porte sur l'expérience utilisateur des 7 pages (Dashboard, Ce week-end, Mon calendrier, Recherche, Favoris, Préférences, À propos), avec la même contrainte stricte "aucune nouvelle fonctionnalité, aucun nouveau provider, aucune évolution métier" déjà appliquée au Sprint 50 pour la dette technique. Audit méthodique de chaque vue + `theme.py`/`strings.py`/`main_view.py` pour 8 axes nommés par le brief (espacements, icônes, titres, textes, boutons, messages vides, dialogues, navigation) — seuls les points "clairement identifiés" corrigés, aucun réagencement spéculatif. **Icônes** : "Mes favoris" affichait `STAR_BORDER` dans son en-tête — un troisième glyphe étoile, distinct des deux (`STAR_OUTLINE`/`STAR`) déjà utilisés pour cette page dans le rail de navigation ; le Dashboard affichait `SPACE_DASHBOARD_OUTLINED` (contour) au lieu de la variante pleine — dans les deux cas corrigé pour rejoindre la convention déjà suivie par "Ce week-end"/"Mon calendrier"/"Recherche"/"Préférences" (icône d'en-tête = variante pleine du rail de navigation) ; la boîte de dialogue de succès affichait un emoji "✅" codé en dur en tête de titre — remplacé par un `ft.Icon(CHECK_CIRCLE, color=Colors.SUCCESS)` dans un `Row`, cohérent avec le reste de l'app qui n'utilise jamais d'emoji en interface, uniquement des icônes Material. **Espacements** : 8 occurrences d'un espacement codé en dur (`spacing=2` ×6, `spacing=4` ×2, trouvées par recherche exhaustive dans tout `gui/`) remplacées par le token `theme.Spacing.XXS` — `theme.py` interdit explicitement ce genre de valeur brute depuis sa création (Sprint 26), ce sprint est le premier à vérifier que la règle est effectivement respectée partout. **Messages vides** : ponctuation des titres `EmptyState` standardisée — `weekend_empty_title`/`dashboard_weekend_championships_empty`/`search_no_results` perdent leur point final pour rejoindre la majorité déjà sans point ; seules les phrases réellement instructives avec un verbe (`weekend_next_hint`, `search_empty_query`) gardent la leur — distinction volontaire entre "titre = label court" et "message secondaire = phrase". **Textes — "À propos"** : affichait un texte statique "Version Alpha" sans aucun numéro, la seule page de l'app qui parlait "de la version" sans dire laquelle, alors que le Dashboard affiche déjà la vraie valeur (`motorsport_calendar.__version__`, section "État", Sprint 53) — `build_about_view()` gagne un paramètre `version` optionnel (défaut : le vrai `__version__`, override réservé aux tests), `about_version` devient un gabarit `"Version {version} — Alpha"`. **Doublons** : `nav_home`/`nav_calendar`, deux chaînes commentées "gardées pour compatibilité" mais jamais référencées nulle part depuis l'introduction de `nav_dashboard`/`nav_my_calendar`, supprimées. **Points identifiés mais non corrigés** (documentés, pas oubliés) : les résultats de "Recherche" ne sont pas cliquables (contrairement aux lignes identiques de l'explorateur de saison "Mon calendrier") — corriger nécessiterait d'ajouter `championship_id`/`event_uid` à `SearchResultItem`, une évolution de `gui/search_service.py` explicitement hors périmètre ("Aucune évolution des services") ; la page "À propos" ne montre pas de `PageHeader` classique (exception assumée depuis le Sprint 28, revalidée plutôt que "corrigée" — un changement aurait été une régression de choix éditorial, pas une correction). 7 nouveaux tests (`test_gui_strings.py` : chaînes mortes absentes, ponctuation, gabarit de version ; `test_gui_views.py` : icônes d'en-tête Favoris/Dashboard, version par défaut/surchargée d'À propos), aucune régression sur les 1961 précédents. **1968 tests total**.

55. **Recherche interactive (Sprint 55)** — objectif : transformer "Recherche" (Sprint 45 — championnats/événements/circuits déjà indexés, mais résultats jusqu'ici purement passifs, identifié comme dette au Sprint 54, piste `-18`) en véritable point d'entrée, en réutilisant exclusivement `SearchService`/`EventDetails`/`CircuitService`/la navigation déjà existante — aucun nouveau service, aucune évolution métier. `gui/search_service.py::SearchResultItem` gagne 3 champs d'identité jamais affichés (`championship_id`/`event_uid`/`circuit_key`), exactement un des trois peuplé selon le type de résultat — même convention "identité portée à travers, jamais le domain object lui-même" que `SeasonEventRow.championship_id`/`event_uid` depuis le Sprint 42 ; `SearchService.build_index()` les peuple simplement en passant (`cid`/`event.event_uid`/`display.circuit_key`, toutes des valeurs déjà calculées pour d'autres besoins), aucune nouvelle logique de recherche. `gui/views/search.py::build_search_view()` gagne 3 callbacks optionnels (`on_championship_click`/`on_event_click`/`on_circuit_click`, chacun `Callable[[SearchResultItem], None] | None`) ; `_result_row`/`_result_section` câblent `card.on_click = lambda e: on_click(item)` — même style d'attribut post-construction que `_championship_button`/`_season_event_row` (calendar.py), jamais un `ft.Container(on_click=...)` au constructeur (qui aurait ajouté à la dette mypy Flet-stub, cf. Sprint 54). **Aucune duplication, la contrainte la plus explicite du brief** : deux résolutions identiques existaient déjà chacune enfouie dans une fermeture locale à une seule boîte de dialogue — extraites en fonctions partagées au niveau de `build_main_view` : `_open_event_details(championship_id, event_uid)` (ex-corps de `_on_event_row_click`, désormais aussi appelée par le clic résultat événement) et `_open_circuit_details(circuit_key)` (ex-corps du `on_circuit_click` interne à `_show_event_details_dialog`, désormais aussi appelé par le clic résultat circuit) — une seule implémentation de chaque résolution, deux appelants chacune. **Clic championnat** : aucune page dédiée par championnat n'existe dans l'app — la "meilleure destination existante" (brief, verbatim) retenue est "Mon calendrier" (`_navigate_to("calendar")`, la même navigation par clé string du Sprint 53), sans jamais toucher `state.selected_championships` — un choix délibéré : cliquer un résultat de recherche pour *regarder* un championnat ne doit jamais modifier silencieusement la sélection de génération de l'utilisateur, une automatisation non demandée par le brief. Tests : `TestSearchResultIdentity` (4 tests, `test_gui_search_service.py`) pour la couche service, 6 nouveaux tests dans `TestSearchView` (`test_gui_views.py`) couvrant explicitement les 4 scénarios nommés par le brief ("clic championnat", "clic événement", "clic circuit", "absence de résultat" — ce dernier en 2 variantes : callback non fourni, et résultats vides malgré des callbacks fournis) plus une garantie d'isolation entre sections (cliquer un résultat championnat ne déclenche jamais les callbacks événement/circuit). Aucune nouvelle fonctionnalité métier, aucun nouveau service, aucune nouvelle dépendance. **1978 tests total**.

56. **Notifications natives (Sprint 56)** — objectif : connecter `NotificationService` (Sprint 46, moteur pur — calcule *quoi*/*quand* notifier, jamais *comment*) à une implémentation native, sans jamais le faire dépendre d'une plateforme. **Audit factuel préalable** (pas une hypothèse) : recherche exhaustive dans le paquet `flet==0.85.3` réellement installé — `flet.controls.services.*` liste 20 services (accelerometer/barometer/battery/browser_context_menu/clipboard/connectivity/file_picker/gyroscope/haptic_feedback/magnetometer/screen_brightness/semantics_service/shake_detector/shared_preferences/share/storage_paths/url_launcher/user_accelerometer/wakelock), aucun pour les notifications système ; `ft.Window` n'expose que `wait_until_ready_to_show`/`destroy`/`center`/`close`/`to_front`/`start_dragging`/`start_resizing`, aucune icône de zone de notification/tray ; le CHANGELOG Dart de Flet (bundlé avec le paquet pub.dev) ne mentionne "notifications" qu'une fois, pour les notifications de **scroll** (concept sans rapport), jamais pour des notifications système, sur aucune version jusqu'à 0.85.3. Conclusion vérifiée : **aucune solution native n'existe dans Flet à ce jour** — conformément au brief ("si aucune solution native propre n'existe : ne pas bricoler"), aucune bibliothèque tierce (plyer, winotify, notify-py, D-Bus/notify2...) n'a été ajoutée pour combler ce manque ; ce choix est documenté comme une décision différée, pas un oubli. **Nouveau `gui/system_notifications.py`** — la seule couche du projet autorisée à connaître l'existence d'une plateforme : `SystemNotifier` (Protocol, 2 méthodes — `is_available()`/`notify(title, body)`), `NullSystemNotifier` (seule implémentation livrée, toujours indisponible par construction), `get_system_notifier()` (factory — toujours la null aujourd'hui, seul endroit où une détection de plateforme future prendrait place), `notify_all(notifications, *, notifier=None)` (point d'entrée unique — ne lève jamais, journalise via `utils.get_logger` — premier vrai consommateur de cette infrastructure de logging, présente mais jamais utilisée depuis sa création). Le titre/corps de notification sont formatés ici (`_format`, avec 5 nouvelles chaînes `strings.py` pour les libellés de `NotificationKind`) — jamais dans `NotificationService`, qui reste "purement structuré, sans texte formaté" exactement comme documenté depuis le Sprint 46. **`gui/controller.py::prepare_notifications()`** (nouveau) — même rôle "le contrôleur relie la logique métier aux préférences, main_view.py appelle seulement le résultat" que `check_for_update` ; la préférence `notifications_enabled` (existante, aucun nouveau réglage) déclenche un court-circuit avant même de construire la liste, exactement où `update_check_enabled` court-circuite déjà `check_for_update` — même patron, pas un nouveau principe. `main_view.py::_prepare_system_notifications()` appelle ce point d'entrée unique une fois au lancement et à chaque rafraîchissement de `year_events` (changement d'année), même déclencheur que les reconstructions d'index recherche/circuits déjà en place — `main_view.py` n'importe jamais `system_notifications` directement. Tests : `TestSystemNotifications` (16 tests) couvrant les 5 scénarios nommés par le brief pour la couche système (disponibles/indisponibles/moteur vide/absence de plateforme compatible, plus la dégradation d'un notifieur qui lève une exception) ; `TestPrepareNotifications` (6 tests, `test_gui_controller.py`) couvrant le scénario restant ("préférences désactivées", prouvé en patchant `compute_notifications` pour échouer si jamais appelé — même technique que `TestCheckForUpdate`) plus le passage bout-en-bout réel (activé, session à venir calculée, mais 0 notification effectivement affichée puisqu'aucune plateforme n'est encore compatible — un fait vérifié en direct par le test, pas supposé). `NotificationService` n'a subi aucune modification. Aucun nouveau provider, aucune évolution métier, aucune nouvelle dépendance, aucun nouveau réglage. **2000 tests total**.

57. **Préparation Beta : Nettoyage & Positionnement (Sprint 57)** — objectif : préparer une application réellement distribuable, uniquement du positionnement produit (aucune fonctionnalité, aucune logique métier, aucune évolution des services). **1. Championnats** : IMSA/WorldSBK masqués des sélecteurs de championnat GUI (Mon calendrier/Mes favoris/Recherche) tant qu'ils n'ont pas de source fiable (`OfficialImsaSource`/`OfficialWorldSbkSource` non implémentées, dette connue depuis les Sprints 36/38/48) — `controller.list_championships()` filtre désormais un nouveau `_HIDDEN_FROM_GUI` (`frozenset({"imsa", "worldsbk"})`) avant de retourner `registry.list_all()` ; c'est le SEUL appelant de cette fonction (vérifié), donc le filtre cascade automatiquement vers `get_groups_for`/`search_service.build_index` sans changement supplémentaire. Aucune suppression de code : `registry.list_all()` lui-même reste intégral, `cli.py generate-imsa`/`generate-worldsbk` et `registry.enabled(config.providers)` (le "championnats actifs" du Dashboard) restent inchangés — cette distinction "exposition GUI" vs "architecture" est délibérée et documentée. `WEEKEND_CHAMPIONSHIP_IDS` (Ce week-end/Dashboard) volontairement NON modifié : IMSA/WorldSBK n'y produisent déjà jamais de carte visible (sources toujours en échec), donc les en retirer n'aurait changé aucun comportement observable pour un risque de scope inutile. **2. À propos** : passe de 5 à 8 lignes de contenu — conserve version réelle/licence/lien GitHub (Sprints 26-54) inchangés, ajoute 3 nouvelles `Section`s (Objectifs du projet, Philosophie Open Source, Technologies utilisées) puisant dans le vocabulaire déjà établi par `docs/PRODUCT_VISION.md` plutôt que d'inventer un nouveau discours marketing. **3. Nouvelle page "Soutenir le projet"** (`gui/views/support.py`, 8ème destination du rail de navigation) : 4 sections exactement comme demandées par le brief — "Soutenir Motorsport Calendar" (PayPal/GitHub Sponsors via `ComingSoonRow`, aucun lien réel), "Voter pour les prochaines fonctionnalités" (8 idées en `theme.chip`, présentation pure, aucun système de vote), "Suggestions" (bouton → GitHub Discussions), "Signaler un problème" (bouton "Signaler un bug" → GitHub Issues). Reçoit `url_launcher` directement (comme `views/about.py` depuis le Sprint 28) plutôt qu'un callback pré-câblé par main_view.py — la seule interactivité de cette page est "ouvrir une URL externe", sans état/logique métier à injecter. **Nettoyage (le nom du sprint, pris au sérieux)** : deux duplications réelles éliminées. `gui/url_opener.py::make_url_opener` (nouveau) remplace 2 implémentations indépendantes du même "ouvrir une URL avec repli Windows" (`views/about.py::on_github_click` depuis le Sprint 26, `main_view.py::_make_release_opener` depuis le Sprint 51/53) — un 3ème site d'appel réel (les boutons Discussions/Issues) a rendu la duplication concrète plutôt que théorique, déclenchant la mutualisation (même principe que `championship_selector.py`). `gui/components/layout::ComingSoonRow` (nouveau) remplace `views/preferences.py::_pref_row` (privé depuis le Sprint 52) — la page "Soutenir le projet" avait besoin de la même forme exacte (icône + label + puce "Disponible prochainement") pour ses emplacements PayPal/GitHub Sponsors. Tests : `TestListChampionships` (+3, IMSA/WorldSBK masqués mais toujours enregistrés), `TestAboutView` (+4), `TestSupportView` (15 tests, `test_gui_views.py`, couvrant les 4 sections + isolation clic), `TestComingSoonRow` (5, `test_gui_components_layout.py`), `test_gui_url_opener.py` (7 tests dont 1 spécifique Windows, skip sur Linux). **2034 tests total**.

58. **Validation Packaging Beta (Sprint 58)** — objectif : audit complet du packaging, uniquement analyse/documentation (aucune modification métier, aucun correctif appliqué). Contexte : le build Linux (`flet build linux motorsport_calendar/gui --module-name app`) venait de se compiler avec succès pour la première fois — Sprint 49 (Packaging Alpha) n'avait jamais dépassé l'étape de compilation (outillage système manquant à l'époque), donc le binaire produit n'avait jamais réellement tourné. **Constat critique, le plus important du sprint** : lancer le binaire compilé produit une plantée immédiate — `~/.cache/com.flet.gui/console.log` contient `ModuleNotFoundError: No module named 'motorsport_calendar'`. Cause racine identifiée en lisant directement le code source de `flet_cli` (`flet_cli/commands/build_base.py`/`utils/project_dependencies.py`), pas supposée : `flet build` cherche un `pyproject.toml` **à l'intérieur du `python_app_path` pointé** (`motorsport_calendar/gui/pyproject.toml`, qui n'existe pas), jamais celui à la racine du projet — résultat, ni les 9 dépendances réelles du projet (`typer`/`rich`/`pydantic`/`icalendar`/`tzdata`/`pyyaml`/`beautifulsoup4`/`lxml`, `httpx` mis à part car partagé avec Flet) ni le paquet `motorsport_calendar` lui-même (seul `gui/` est embarqué dans `app.zip`, jamais `core/`/`providers/`/`config/`/`cache/`/`exporters/`) ne sont installés dans le build. Deux corrections candidates documentées (`docs/PACKAGING.md` §6.3, aucune appliquée ce sprint) : ajouter une `pyproject.toml` dédiée dans `gui/`, ou relancer `flet build` depuis la racine du projet avec `tool.flet.app.path` pointant vers `gui/` (réutilisant la `pyproject.toml` racine déjà correcte). **Cartographie complète du dossier produit** (112 Mo au total) : `lib/` 88 Mo (`libflutter_linux_gtk.so` 42 Mo, `libpython3.12.so.1.0` 31 Mo, `libapp.so` 16 Mo, plugins Flet natifs), `python3.12/` 13 Mo (stdlib CPython embarquée), `site-packages/` 7,3 Mo (uniquement les dépendances de Flet lui-même), `data/` 3,9 Mo (assets Flutter + `app.zip`). **Autonomie confirmée pour la partie qui fonctionne** : Python est entièrement embarqué (`libpython3.12.so.1.0` + stdlib compilée, `ldd`/`libapp.so` confirment "statically linked", aucun Python système requis) ; les bibliothèques système (GTK3/GLib/Pango/Cairo/ATK/X11/Wayland) résolues par `ldd` sont toutes déjà présentes sur un Ubuntu Desktop standard, aucune installation supplémentaire nécessaire sur la machine cible (une fois le blocage corrigé). **Identité de l'application encore générique** : nom de fenêtre GTK natif compilé en dur à `"gui"` (`my_application.cc`), nom d'exécutable `gui` et ID d'application `com.flet.gui` (`CMakeLists.txt`), version embarquée `1.0.0` au lieu de `0.2.0` (`data/flutter_assets/version.json`) — tous des défauts génériques de Flet, faute de `pyproject.toml`/section `[tool.flet]` au bon endroit (même cause racine que le blocage principal). Aucun fichier `.desktop` Linux nulle part (pas d'entrée dans le menu Applications). **Bonne nouvelle confirmée** : les chemins de préférences (`~/.config/motorsport-calendar/gui_prefs.json`), de cache (`~/.cache/motorsport-calendar/`) et de `config.yaml` (Sprint 49, `utils/paths.py`) restent corrects et totalement indépendants des variables de stockage propres à Flet (`FLET_APP_STORAGE_DATA`/`FLET_APP_STORAGE_TEMP`, jamais utilisées par le code de l'app). **Nouveau `docs/RELEASE.md`** : procédure complète (générer Linux, générer Windows, publier une Release GitHub via `gh release create`), arborescence `Release/` proposée (`Linux/`/`Windows/`/`Source/` + `CHANGELOG.md`/`LICENSE`/`README.md`, archives `.tar.gz`/`.zip` + `SHA256SUMS`, jamais les dossiers bruts) ; `Release/` ajouté à `.gitignore`. `docs/PACKAGING.md` corrigé : l'affirmation du Sprint 49 ("vérifié... correct de bout en bout") était prématurée, jamais réellement testée en conditions réelles à l'époque — corrigée avec un encart explicite plutôt que silencieusement réécrite. Aucune régression : suite de tests inchangée (2034, dont 1 skip Windows-only), aucun fichier source applicatif modifié.

59. **Correction du packaging Flet (Sprint 59)** — objectif (mission directe, pas un brief de sprint numéroté par l'utilisateur, mais documentée avec la même rigueur) : corriger le blocage identifié au Sprint 58, en identifiant d'abord la méthode officiellement recommandée par Flet, sans bricolage ni casse du flux de développement actuel. **Découverte en cours de route** : le diagnostic du Sprint 58 (dépendances manquantes) était correct mais incomplet — l'appliquer seul (une `pyproject.toml` dupliquant les 9 dépendances dans `gui/`) a été testé pour de vrai (rebuild + relance du binaire) et a produit exactement la même erreur `ModuleNotFoundError`. Cause additionnelle identifiée : `flet build` zip le *contenu* du dossier ciblé (`motorsport_calendar/gui/`), aplati, sans jamais l'envelopper dans un paquet `motorsport_calendar.` — aucune liste de dépendances, aussi complète soit-elle, ne fait résoudre un import absolu (`from motorsport_calendar.gui... import`) contre un bundle aplati. **Correctif retenu** : `motorsport_calendar/gui/pyproject.toml` (nouveau fichier, manifeste de build uniquement, jamais lu par pip/hatchling pour l'installation normale) déclare exactement 2 dépendances (`flet`, `motorsport-calendar`) et utilise `tool.flet.dev_packages` — le mécanisme officiel de Flet pour "une dépendance développée localement, pas encore sur PyPI" (confirmé en lisant `flet_cli/commands/build_base.py`) — pour installer le projet lui-même depuis sa racine réelle (`../..`) comme un vrai paquet pip, correctement imbriqué, via la configuration hatchling déjà existante à la racine (`packages = ["motorsport_calendar"]`, rien de nouveau à maintenir). Installer le projet ainsi résout aussi ses propres dépendances déclarées de façon transitive — **une seule source de vérité**, jamais une liste dupliquée à synchroniser manuellement (contrairement à la première tentative). La commande de build documentée reste inchangée (`flet build linux motorsport_calendar/gui --module-name app`), tout comme son emplacement de sortie — le scaffold Flutter existant a donc pu être réutilisé pour un rebuild rapide (~25 secondes) plutôt que régénéré intégralement. **Vérifié pour de vrai, pas supposé** : rebuild complet exécuté deux fois (une fois par tentative), `site-packages/motorsport_calendar/` confirmé correctement imbriqué (`core/`/`providers/`/`config/`/`cache/`/`exporters/`/`gui/`, version `0.2.0`), binaire relancé deux fois (`./motorsport-calendar`, nom corrigé) — aucune trace d'erreur dans `~/.cache/motorsport-calendar/console.log` (vide les deux fois, contre une trace systématique avant le correctif), processus resté actif bien au-delà du point où l'ancien build plantait instantanément. Identité de l'application corrigée au passage (même cause racine) : exécutable/ID d'application/titre de fenêtre natif passent de `gui`/`com.flet.gui`/`"gui"` à `motorsport-calendar`/`com.flet.motorsport-calendar`/`"motorsport-calendar"`. **Non vérifié** : aucune confirmation visuelle réelle d'une fenêtre rendue (pas de compositeur d'affichage réel dans cet environnement, même limitation que chaque sprint GUI) — mais le crash Python au démarrage, le bug concret demandé à corriger, est définitivement résolu. **Non résolu** : la version embarquée dans le build affiche encore `1.0.0` au lieu de `0.2.0` malgré `project.version = "0.2.0"` dans le manifeste — piste non retracée jusqu'à sa cause exacte (cosmétique, n'affecte ni le démarrage ni le comportement de l'app). `docs/PACKAGING.md` §6 conservé intact comme trace historique de l'audit ; §7 (nouveau) documente le correctif. `docs/RELEASE.md` mis à jour (l'avertissement de blocage retiré, remplacé par une confirmation). Tests : `TestFletBuildManifest` (8 tests, `tests/test_packaging.py`) — garde-fou vérifiant que le manifeste de build ne redéclare jamais les dépendances racine (seulement `flet` + le projet lui-même), que `project.name`/`version` restent synchronisés avec la racine, et que la redirection `tool.flet.dev_packages` pointe vers un vrai projet installable. Aucune modification métier : `pyproject.toml` racine jamais touché (diff `git diff pyproject.toml` confirmé provenir uniquement de dérive historique non commise, antérieure à cette session) ; `motocal`/`motocal-gui`/`pip install -e .[gui]` re-testés et confirmés inchangés. **2041 tests total**.

---

## Fonctionnalités en cours / prochaines

**Prochaines tâches recommandées** (après Sprint 59) :

-23. **Retracer pourquoi la version embarquée du build reste `1.0.0`**
    malgré `project.version = "0.2.0"` dans `motorsport_calendar/gui/
    pyproject.toml` (Sprint 59, non résolu, cosmétique) — `flet_cli`
    lit bien `project.version` et le passe en `--build-name` à Flutter,
    mais `data/flutter_assets/version.json` ne semble pas être le
    fichier alimenté par ce flag ; à investiguer plus finement (peut-être
    `pubspec.yaml` généré, ou un flag CLI `--build-version` séparé à
    passer explicitement) si l'exactitude de ce fichier devient un jour
    nécessaire.
-22bis. **Vérifier le correctif Sprint 59 sur le build Windows** — le
    même manifeste/mécanisme devrait s'appliquer identiquement (aucune
    logique spécifique à Linux dans le correctif), mais jamais exécuté
    sur une vraie machine Windows dans cet environnement.
-22ter. **Vérification visuelle réelle du binaire Linux corrigé** — le
    crash Python est résolu et vérifié, mais aucun rendu de fenêtre réel
    n'a pu être confirmé (pas de compositeur d'affichage dans cet
    environnement) ; à confirmer sur un poste avec affichage avant toute
    distribution publique.
-22quater. **Ajouter un fichier `.desktop` Linux** — aucune entrée de menu
    Applications aujourd'hui, l'utilisateur doit lancer l'exécutable
    directement ; pas nécessaire pour une Beta distribuée en archive
    `.tar.gz` mais attendu pour une intégration desktop réelle
    (`.deb`/AppImage/Flatpak, hors périmètre actuel).

-21. **Fournir de vrais liens PayPal/GitHub Sponsors** (Sprint 57 a posé
    la structure, "aucun lien réel n'est encore nécessaire" par le
    brief) — dès qu'une décision produit est prise d'accepter des dons,
    remplacer les deux `ComingSoonRow` de "Soutenir le projet" par de
    vrais boutons `make_url_opener`, exactement comme Discussions/Issues
    le sont déjà.
-20bis. **Réintégrer IMSA/WorldSBK dans le sélecteur GUI** dès qu'une
    source fiable existe pour l'un des deux (voir ADR-027/ADR-029) — il
    suffit de retirer l'id concerné de
    `controller.py::_HIDDEN_FROM_GUI`, aucun autre changement requis.



-20. **Implémenter un vrai `SystemNotifier`** (Sprint 56 a posé
    l'abstraction, jamais une implémentation) — dès qu'une solution
    native propre existe (Flet ajoute un service de notification, ou une
    décision délibérée est prise d'ajouter une dépendance tierce —
    `plyer`/`winotify`/`notify-py`/D-Bus selon la plateforme), un nouveau
    `SystemNotifier` concret vient remplacer `NullSystemNotifier` dans
    `get_system_notifier()` — le seul endroit à modifier, tout le reste
    de l'app (moteur, préférences, orchestration) reste inchangé par
    construction.
-19bis. **Réévaluer périodiquement les nouvelles versions de Flet** pour
    une éventuelle notification système officielle — l'audit du Sprint 56
    est un instantané de la version 0.85.3 installée, pas une garantie
    permanente ; à revérifier avant toute Beta si Flet publie une version
    majeure entretemps.

-19. **Vérification visuelle réelle de la recherche interactive** (Sprint
    55) — 3 nouveaux types de clic (championnat/événement/circuit) jamais
    exercés sur un poste avec affichage réel ; structure + 10 tests nets
    vérifiés, aucun rendu/clic pixel confirmé.

-17. **Vérification visuelle réelle du nouveau Dashboard** (Sprint 53) —
    3 nouvelles sections ("Nouveautés"/"Accès rapides"/"État") jamais rendues sur un
    poste avec affichage réel ; structure des contrôles + 29 tests nets vérifiés,
    aucun rendu pixel confirmé — même limitation que chaque sprint GUI précédent
    (voir `-16` ci-dessous pour la page Préférences, jamais résolue non plus).
-17bis. **Publier un vrai manifeste de mise à jour** (`config.update.manifest_url`,
    Sprint 51) — la carte "Nouveautés" du Dashboard est fonctionnelle et testée mais
    reste un no-op silencieux tant qu'aucune URL réelle n'est configurée ; maintenant
    que la carte existe en plus de la boîte de dialogue de démarrage, ce manque se
    voit à deux endroits au lieu d'un seul.

-16. **Vérification visuelle réelle de la page Préférences** (Sprint 52) —
    structure des contrôles + 9 tests nets vérifiés, aucun rendu pixel confirmé
    (Switch/Dropdown jamais utilisés dans ce projet avant ce sprint) ; même
    limitation que chaque sprint GUI précédent, poste avec affichage indisponible
    dans cet environnement.
-15bis. **Implémenter réellement Thème/Langue/Format horaire** — `PreferencesModel`
    (Sprint 52) porte déjà le typage (`theme`/`language`/`time_format`) mais rien
    n'est persisté ni lu ailleurs dans l'app ; la page Préférences les affiche
    volontairement en "Disponible prochainement" (brief explicite : "préparer, sans
    forcément implémenter").
-15. **Publier un vrai manifeste de mise à jour** — `config.update.manifest_url`
    reste vide par défaut (aucune URL codée en dur, Sprint 51) ; pour que la
    vérification produise un résultat réel en usage, il faut héberger un
    `manifest.json` quelque part (n'importe quel hébergeur statique convient — HTTP
    GET simple, aucune API particulière requise) et documenter l'URL choisie dans
    `config.yaml`/`docs/PACKAGING.md`.
-14ter. **Vérification visuelle réelle de la boîte de dialogue "nouvelle version
    disponible"** — structure des contrôles vérifiée par les tests, rendu pixel non
    confirmé (même limitation que chaque sprint GUI précédent, poste avec affichage
    indisponible dans cet environnement).
-14. **Découpage de `gui/main_view.py::build_main_view`** (771 lignes, 89 % du
    fichier) — seule vraie anomalie de taille du projet identifiée par l'audit Sprint
    50 (`docs/AUDIT.md` §6). Piste : extraire chaque `_show_*_dialog` (déjà des
    fonctions imbriquées bien délimitées, ~55-86 lignes) et le câblage de navigation
    vers des fonctions module-level, à l'image de `views/about.py`/`calendar.py`/
    `search.py`. Nécessite une vérification visuelle réelle (poste avec affichage),
    pas un découpage à l'aveugle — sprint dédié, pas une tâche annexe.
-13quater. **Réévaluer la dette mypy Flet stub-version** (23 erreurs source + une
    bonne part des 157 en tests, `docs/AUDIT.md` §4) si/quand une version de Flet
    dont les stubs matchent enfin le runtime 0.85+ devient disponible — pourrait
    faire tomber ce solde d'un coup, sans toucher une ligne de code applicatif.
-13ter-bis. **Mesurer la mutualisation des clients HTTP entre providers** (une
    instance `httpx.AsyncClient` partagée plutôt qu'une par source) et la
    virtualisation des longues listes GUI — identifiées par l'audit Sprint 50
    (`docs/AUDIT.md` §8) comme pistes de performance non mesurées faute de réseau/
    affichage réel dans cet environnement.
-13. **Terminer le build Linux** — `flet build linux motorsport_calendar/gui
    --module-name app` atteint l'étape de compilation native mais reste bloqué par
    l'outillage système manquant sur cette machine (`binutils clang cmake llvm lld
    ninja-build pkg-config libgtk-3-dev libunwind-dev`) ; une fois installé (voir
    `docs/PACKAGING.md` §2), relancer la même commande (SDK Flutter déjà en cache) et
    exécuter la checklist de validation complète du brief Sprint 49 (démarre, charge les
    assets, ouvre toutes les pages, lit/écrit les préférences, crée le cache, génère un
    ICS, fonctionne sans le dépôt Git) contre le binaire réellement compilé.
-13bis. **Exécuter le build Windows** — jamais lancé faute de machine Windows dans cet
    environnement ; procédure documentée dans `docs/PACKAGING.md` §3 (Visual Studio 2022+
    avec workload "Desktop development with C++", Developer Mode activé) mais jamais
    vérifiée en conditions réelles.
-13ter. **Câbler `theme.logo_placeholder()` sur les vraies images** (`gui/assets/logo/
    mc-icon.svg`, `logo-horizontal.svg`, `logo-vertical.svg`, livrées Sprint 49) —
    remplacer les placeholders visuels par les assets officiels est un chantier de Design
    System, explicitement hors périmètre du brief Sprint 49 ("aucun changement de Design
    System").
-12. **`generate-imsa`/`generate-worldsbk`** restent des commandes CLI qui échouent
    systématiquement (`NotImplementedError`) — aucune source exploitable trouvée après
    trois investigations indépendantes (Sprints 36/38, ré-vérifiées Sprint 48). Pistes
    non épuisées : automatisation navigateur (Playwright, changement d'ampleur des
    dépendances du projet) pour intercepter les vrais appels XHR de worldsbk.com ou
    contourner le blocage Cloudflare d'imsa.com ; contact direct IMSA/Dorna pour un accès
    API partenaire ; surveiller si l'un des deux publie un jour un flux structuré. Voir
    `docs/DATA_SOURCES.md` sections IMSA/WorldSBK et ADR-027/ADR-029/ADR-039.
-11bis. **Étendre le provider WEC** — pistes pour un futur sprint : ancrer le
    découpage vendredi-dimanche sur le fuseau du circuit plutôt qu'UTC (limitation déjà
    documentée, partagée avec "Ce week-end", pas spécifique à WEC) ; vérifier
    périodiquement si un nouveau nom de course WEC échappe au motif "X Hours" et à la
    table `_NAMED_RACE_DURATION_HOURS` (repli générique à 6h actuellement, correct pour
    la saison 2026 mais à surveiller sur les saisons futures) ; compléter
    `WEC_ADDRESS_COUNTRY_CODES`/`WEC_CIRCUIT_DATA` au fil des nouveaux circuits (ex.
    Silverstone, déjà visible sur le calendrier 2027 mais pas encore dans la table).
    Aucun n'est nécessaire pour cette première version, volontairement gardée simple.
-11. **Une page "Circuits" dédiée** — aujourd'hui la fiche Circuit (Sprint 47) ne
    s'ouvre que depuis un clic sur le nom du circuit dans la fiche événement ; il
    n'existe aucune destination de navigation listant `CircuitService.list_circuits()`
    dans son ensemble (contrairement à "Recherche", qui expose déjà chaque circuit comme
    résultat individuel, Sprint 45). Piste pour un futur sprint "valeur" — même patron
    Layout System (`PageHeader`/`Section`/`CardList`) que les pages existantes.
-10bis. **Étendre la fiche Circuit** — pistes pour un futur sprint : clic sur une ligne
    de l'historique des événements ouvrant directement la fiche événement correspondante
    (`CircuitEventEntry` porte déjà `championship_id`/`event_uid`, jamais interprétés à
    ce jour — même patron "carry identity, never interpret" que `SeasonEventRow`,
    Sprint 42) ; carte/logo du circuit si un jour une source de données l'apporte.
    Aucun n'est nécessaire pour cette première version, volontairement gardée simple.
-10. **Câbler `NotificationService` dans l'interface** — le moteur (Sprint 46) est
    fonctionnel et testé mais n'est consommé nulle part : ni page dédiée, ni destination
    de navigation, ni notification système réelle (Windows/Linux/macOS), explicitement
    hors périmètre de ce sprint ("construire les fondations" uniquement). Pistes pour un
    futur sprint : une page "Notifications" listant les prochaines échéances (même
    patron Layout System que "Recherche", Sprint 45) et/ou une véritable notification
    système par plateforme, consommant `NotificationService.compute_notifications()` tel
    quel sans y toucher.
-9bis. **Étendre le moteur de notifications** — pistes pour un futur sprint : préférence
    persistée pour les `kinds` activés (aujourd'hui un paramètre d'appel uniquement, pas
    encore une préférence comme `lead_times`/`favorites_only`) ; plusieurs délais
    persistés simultanément plutôt qu'un seul "délai par défaut" ; un vrai calcul de
    "début de week-end" distinct de "première session" si un jour la donnée le permet
    (voir ADR-037). Aucun n'est nécessaire pour cette première version, volontairement
    gardée simple (fondations demandées, rien de plus).
-9. **Vérification visuelle réelle de "Recherche"** sur un poste avec affichage —
    structure des contrôles + 46 tests vérifiés (dont un smoke test en direct sans mock
    contre chaque exemple explicite du brief : spa/Spa/SPA/spa francorchamps, Le
    Mans/lemans, Moto/MotoGP, Formula, GT), aucun rendu pixel confirmé. Attention
    particulière : la recherche instantanée à chaque frappe (`on_change`) reste-t-elle
    fluide visuellement sur un jeu de données complet (17 championnats × plusieurs
    centaines d'événements/circuits), et le `TextField` conserve-t-il le focus clavier
    après chaque `page.update()` déclenché par une frappe ?
-8bis. **Étendre la recherche** — pistes pour un futur sprint : clic sur un résultat
    ouvrant directement la fiche événement (Sprint 42) ou la page du championnat,
    historique des dernières recherches, ou élargir la portée au-delà de l'année
    actuellement parcourue sur "Mon calendrier" (aujourd'hui volontairement limité à
    `year_events`, cohérent avec "aucun appel réseau supplémentaire"). Aucun n'est
    nécessaire pour cette première version, volontairement gardée simple.
-8. **Vérification visuelle réelle de "Mes favoris"** sur un poste avec affichage —
    structure des contrôles + 36 tests vérifiés (dont le pipeline complet Dashboard/Ce
    week-end/Mon calendrier revérifié en direct sans mock), aucun rendu pixel confirmé.
    Attention particulière : le sous-titre "N favoris" du `PageHeader` reste-t-il lisible
    à 0/1/plusieurs favoris (accord singulier/pluriel) ?
-7bis. **Étendre les favoris** — pistes pour un futur sprint : synchroniser
    `PreferencesModel.favorite_championships` (champ déjà anticipé depuis les tout
    premiers sprints, jamais branché) avec `FavoritesService` le jour où la page
    Préférences devient réelle ; notification/rappel pour les favoris ; export ICS
    dédié "mes favoris uniquement" en un clic depuis le Dashboard. Aucun n'est
    nécessaire pour cette première version, volontairement gardée simple.
-7. **Vérification visuelle réelle de la refonte "Mon calendrier"** sur un poste avec
    affichage — le sprint le plus visuellement significatif à ce jour (page entièrement
    réorganisée), structure des contrôles + ~65 tests vérifiés mais **aucun** rendu pixel
    confirmé. Points d'attention spécifiques : le pied de page fixe (destination +
    "Créer") reste-t-il visible sans jamais nécessiter de scroll, même sur la plus petite
    fenêtre supportée (`page.window.min_height = 580`) ? Les accordéons `ft.ExpansionTile`
    s'animent-ils correctement ? Le bouton championnat sélectionné (`theme.card(selected=
    True)`) est-il visuellement assez distinct du non-sélectionné pour être compris sans
    ambiguïté ?
-6bis. **Étendre la refonte "Mon calendrier"** — pistes pour un futur sprint : mémoriser
    l'état ouvert/fermé des accordéons dans les préférences utilisateur (aujourd'hui
    remis à zéro à chaque lancement, sauf la catégorie de la présélection) ; réduire la
    largeur du pied de page fixe sur petite fenêtre ; indicateur visuel (badge) du nombre
    de championnats sélectionnés directement sur le titre de chaque accordéon. Aucun
    n'est nécessaire pour cette première version, volontairement gardée simple.
-6. **Vérification visuelle réelle de la fiche événement** sur un poste avec affichage
    (structure des contrôles Flet + 20 tests vérifiés, rendu pixel non confirmé — même
    limitation que chaque sprint GUI précédent). Attention particulière à vérifier : la
    boîte de dialogue (`ft.AlertDialog`, largeur fixe 400px) avec un événement à
    beaucoup de sessions (ex. week-end MotoGP à 6 sessions) reste lisible, et le scroll
    interne (`ft.ScrollMode.AUTO` sur la colonne de contenu) fonctionne réellement.
-5bis. **Étendre la fiche événement** — pistes naturelles pour un futur sprint : action
    "Exporter cet événement seul" en ICS, lien retour vers "Ce week-end" si l'événement
    concerné est celui du week-end en cours, ou favoris par événement (distinct des
    favoris par championnat déjà anticipés). Aucun n'est nécessaire pour cette première
    version, volontairement gardée simple (exactement les champs demandés par le brief).
-5. **Vérification visuelle réelle de l'explorateur de saison** sur un poste avec
    affichage (structure des contrôles Flet + 23 tests vérifiés, rendu pixel non confirmé
    — même limitation que chaque sprint GUI précédent). Attention particulière à
    vérifier : une sélection large (17 championnats × ~10 événements chacun) reste
    lisible/scrollable dans le gabarit `page_shell` existant (scroll de page déjà en
    place, jamais testé avec une liste de cette longueur avant ce sprint).
-4bis. **Filtre texte/recherche dans l'explorateur de saison, ou repli automatique des
    mois passés** — pistes pour un futur sprint (le lien direct vers le détail d'un
    événement, également listé ici depuis le Sprint 41, est désormais fait — Sprint 42).
    Aucun n'est nécessaire pour cette première version, volontairement gardée simple.
-4. **Vérification visuelle réelle du résumé de sélection** sur un poste avec affichage
    (Sprint 40 n'a pu valider que la structure des contrôles Flet, pas le rendu pixel —
    même limitation que chaque sprint GUI précédent).
-3bis. **Étendre le résumé de sélection** — pistes naturelles pour un futur sprint :
    répartition par championnat à l'intérieur du résumé (ex. "F1 : 26 événements"), lien
    direct entre le résumé et le récapitulatif de l'étape 4 (aujourd'hui deux blocs
    distincts, juxtaposés), ou persister `year_events` d'une année à l'autre (cache
    en mémoire) pour éviter un rechargement complet en cas d'aller-retour entre deux
    années déjà visitées dans la même session. Aucun n'est nécessaire pour cette première
    version, volontairement gardée simple (comptages + période, rien de plus).
-3. **Vérification visuelle réelle du Dashboard** sur un poste avec affichage (Sprint 39
    n'a pu valider que la structure des contrôles Flet, pas le rendu pixel — même
    limitation que chaque sprint GUI précédent, voir points similaires ci-dessous pour
    "Ce week-end").
-2bis. **Étendre le Dashboard** — pistes naturelles pour un futur sprint "valeur" :
    lien direct depuis une stat card vers l'onglet correspondant (ex. cliquer "Prochain
    week-end" ouvre "Ce week-end"), un mini-historique (derniers événements passés), ou
    un raccourci vers "Mon calendrier" pré-rempli avec les championnats du prochain
    week-end. Aucun n'est nécessaire pour cette première version, volontairement gardée
    simple (6 informations demandées, rien de plus).
-2. **Trouver une source WorldSBK exploitable** — `OfficialWorldSbkSource` reste un stub
    (`NotImplementedError`) après investigation exhaustive au Sprint 38 (calendrier
    entièrement rendu côté client, hôte API candidat `wsbk-api-origin.gplat-test.
    pulselive.com` injoignable depuis l'extérieur). Ré-investigué au Sprint 48 : deux
    nouveaux hôtes Pulselive découverts (`api.wsbk.pulselive.com` — réel, mais aucune
    route testée n'existe ; `wsbk.pulselive.com` — un simple miroir SPA du site, pas une
    API), même conclusion. Pistes non explorées : automatisation navigateur (Playwright),
    contact direct Dorna/WorldSBK pour un accès API partenaire, ou surveiller si un hôte
    API public équivalent à `api.pulselive.motogp.com` apparaît un jour pour WorldSBK.
    Voir `docs/DATA_SOURCES.md` section WorldSBK et ADR-029/ADR-039.
-1. **Compléter `sro_series/circuit_data.py`** au fil des nouveaux circuits GT rencontrés
    sans mapping (même pattern de repli propre — nom depuis `feature__heading`, fuseau
    `"UTC"` — que les autres championnats). Table actuelle couvre les venues observées au
    Sprint 37 ; SRO ajuste parfois son calendrier en cours de saison (nouveaux circuits,
    remplacements).
-0bis. **Vérifier périodiquement les rounds ignorés faute de timetable publié** (ex.
    Indianapolis 8 Hour 2026 chez GT World Challenge America au moment du Sprint 37) — SRO
    publie ses horaires détaillés en général quelques semaines avant chaque manche ;
    `get_season()` les inclura automatiquement dès qu'ils apparaissent, aucun changement de
    code nécessaire.
0. **Trouver une source IMSA exploitable** — `OfficialImsaSource` reste un stub (`NotImplementedError`) après investigation exhaustive au Sprint 36 (imsa.com bloqué par Cloudflare, Al Kamel = résultats post-course seulement, Wikipedia = pas d'horaires de session, Sportscar365 = prose non structurée). Ré-investigué au Sprint 48 : imsa.com bloque désormais jusqu'à son propre `robots.txt`/`sitemap.xml` (blocage plus strict qu'au Sprint 36), Al Kamel et Wikipedia inchangés, même conclusion. Pistes non explorées : automatisation navigateur (Playwright, changement d'ampleur des dépendances du projet), contact direct IMSA/SRO pour un accès API partenaire, ou surveiller si IMSA publie un jour un flux structuré. Voir `docs/DATA_SOURCES.md` section IMSA et ADR-027/ADR-039.

1. **Intégrer le logo définitif** — remplacer les appels `theme.logo_placeholder(...)` listés dans `gui/assets/logo/README.md` dès que les SVG du Brand Set v1.0 sont livrés dans le dépôt (un seul emplacement : À propos)
1bis. **Déposer les logos officiels de championnat** — copier `formula1.png`/`formula2.png`/`formula3.png`/`f1-academy.png`/`wec.png` dans `gui/assets/championships/` (voir README dans ce dossier) puis décommenter `assets_dir=` dans `gui/app.py` (partagé avec le point 1) : `get_championship_asset()` détecte les fichiers automatiquement, aucun autre changement de code nécessaire. `formula-e` n'a pas encore de ligne dans `_LOGO_FILENAMES` (Sprint 34, volontaire) — l'ajouter en même temps si un logo Formula E est fourni.
2. **Packaging Windows `.exe`** — `flet build windows` → distributable sans Python
3. **Fonctionnalités "Mes favoris"** — sauvegarder/charger une liste de championnats favoris ; composer sa vue avec `PageContainer(header=PageHeader(...), body=[Section(CardList([build_championship_card(...) for ...]))])` — le Layout System (Sprint 31) et ChampionshipCard (Sprint 30) couvrent déjà tout le nécessaire, plus aucun code de mise en page à écrire
4. **Activation Préférences** — brancher `PreferencesModel` sur l'UI (dropdowns actifs)
5. **Audit mypy `main_view.py`** — signatures `on_click` Flet 0.80 (code) vs 0.85.3 (installé) : 17 erreurs préexistantes (même famille depuis Sprint 26) — Sprint 44 ajoute 2 occurrences de plus du schéma déjà toléré `Page.X_load_task` (`weekend_reload_task`/`dashboard_reload_task`, déclenchés au basculement d'un favori), pas de nouvelle catégorie. `gui/views/calendar.py` passe à 0 erreur (0 !) : l'accordéon (`_category_accordion`'s `ft.ExpansionTile.on_change`, 2 erreurs de la même famille) a déménagé vers `gui/components/championship_selector.py` avec l'extraction Sprint 44 — dette relocalisée, pas nouvelle, total du projet inchangé
6. **Vérification visuelle réelle** — confirmer sur un poste avec affichage : le redimensionnement du gabarit, le rendu du nouvel en-tête séparé (Favoris/Ce week-end/Préférences), les bordures restaurées des lignes de Préférences, et les cartes ChampionshipCard réelles de Ce week-end (métadonnées normalisées, Sprint 32)
7. ✅ **`OfficialWecSource` réelle** — résolu Sprint 48 (`AcoSportsEventSource`, fiawec.com), voir entrée #48 de l'historique ci-dessus
8. **Ancrer le découpage vendredi-dimanche sur le fuseau du circuit** (actuellement UTC) — cas limite documenté pour les circuits très à l'est (Japon, Singapour, Chine, Australie) dont la séance du vendredi matin local peut tomber un "jeudi" UTC
9. **Prochains composants de la bibliothèque** — au fur et à mesure des besoins (Recherche, Tableau de bord, Notifications, Historique), ajouter à `gui/components/` en suivant le même principe : modèle propre au composant, aucune dépendance à un domaine métier, primitives `theme.py` uniquement. Pour une page de mise en page, partir de `gui/components/layout/` — c'est exactement pour ça qu'il existe.
10. **Premier consommateur réel de `SectionHeader`** — dès qu'une page affichera plusieurs groupes de cartes distincts (Tableau de bord probablement en premier), l'utiliser à l'intérieur d'une `Section` par groupe.
11. **Prochain championnat candidat** — Porsche Supercup est déjà anticipée dans `display_names.py` (nom lisible présent, non enregistrée dans `ProviderRegistry`). Porsche Supercup partage vraisemblablement le dataset f1calendar (mentionné dans la docstring de `F1CalendarBaseSource`) — suivre exactement le patron Formula E (Sprint 34) : vérifier le vrai schéma JSON avant d'écrire quoi que ce soit, ne jamais supposer.
12. ✅ **`OfficialWecSource` réelle via `AcoSportsEventSource`** — résolu Sprint 48 : une vraie manche WEC (pas un Prologue) confirme exactement la même structure JSON-LD qu'ELMS/MLMC, voir entrée #48 de l'historique ci-dessus
13. **`AcoSportsEventSource.get_season(year)` limité à la saison courante** — `europeanlemansseries.com`/`lemanscup.com` ne publient pas d'archive de saisons passées à une URL prévisible (`/en/season/{year}` 404 pour toute année ≠ année courante, vérifié Sprint 35). Comportement actuel : propage `httpx.HTTPStatusError` (404), cohérent avec le reste du projet — pas de contournement prévu, limitation du site lui-même.
11. **Corriger `_build_circuit` côté provider** (F2/F3/F1 Academy) — utiliser `event_data["location"]` pour `Circuit.name` au lieu de réutiliser `event_data["name"]` ; réduirait le besoin de repli sur `circuit.city` dans `event_display.py`, mais nécessite de toucher au provider (explicitement hors périmètre du Sprint 32).
12. **Compléter les tables `_CIRCUIT_DATA`** (pays) de F2/F1 Academy — F3 couvre déjà mieux certains circuits ; réduirait le nombre de lignes pays masquées dans "Ce week-end" (côté provider, hors périmètre du Sprint 32).
13. **Table démonyme pays → adjectif** si la qualité "Canada Grand Prix" vs "Canadian Grand Prix" devient gênante — actuellement un compromis assumé (`event_display.py`).

---

## Conventions importantes

- **Imports lazy dans la CLI** : tous les imports de providers/exporters sont à l'intérieur de la fonction de commande.
- **Mock HTTP** : `AsyncMock(side_effect=[meetings, sessions])` patché sur `OpenF1Source._get_json` pour les tests CLI. Client injecté directement pour les tests unitaires source.
- **Timezone** : OpenF1 retourne UTC. Le fuseau IANA est dérivé de `circuit_short_name` via `_CIRCUIT_TZ_MAP` dans `openf1.py`. Fallback : `"UTC"`.
- **event_uid** : `openf1-meeting-{meeting_key}@motorsport-calendar`
- **UID VEVENT** : `{event_uid}-{session.type}`
- **Sessions invalides** : `_build_session()` retourne `None` si date manquante, naive, ou end ≤ start.
- **Cache** : `HttpCache(cache_dir, ttl)` — fichiers `{sha256}.json` dans `.cache/`. Quand `OpenF1Source(client=mock)` est utilisé en test, le cache est désactivé automatiquement (client injecté = mode test).
- **`--refresh`** : passe `refresh=True` à `OpenF1Source`, propagé à `HttpCache.get_json(refresh=True)`.
- **WEC SessionTypes supportés** : `FREE_PRACTICE`, `QUALIFYING`, `HYPERPOLE`, `RACE` — tous déjà dans `SessionType` (StrEnum).
- **WEC Championship** : `id=f"wec-{year}"`, `name="FIA World Endurance Championship"`, `category=ChampionshipCategory.ENDURANCE`.
- **Config** : `ConfigService(config_path=None)` — cherche `config.yaml` dans CWD puis `~/.config/motorsport-calendar/`. Aucun fichier → défauts complets.
- **config.yaml** : ignoré par git (personnel). `config.example.yaml` commité comme référence.
- **VALARM** : `IcsExporter(alarm_minutes=N)` — N>0 → `TRIGGER:-PTNm` dans chaque VEVENT. CLI lit `config.ics.alarm_minutes`.
- **Data Acquisition Layer (DAL)** : `core/datasource/` — `DataSource` (ABC marker), `JsonDataSource` (abstract `fetch_json(url, params)`), `HtmlDataSource` (abstract `fetch_html(url)`), `IcsDataSource` (abstract `fetch_ics(url)`). Les sources implémentent l'interface DAL de leur catégorie **en plus** de l'interface domaine (`Formula1Source`, etc.). `OpenF1Source` et `F1CalendarSource` implémentent `JsonDataSource`.
- **F1CalendarSource (F2)** : config F2 uniquement — `_series_key="f2"`. `_SESSION_MAP` accepte 6 clés : `fp1` et `practice` → `FP1` (renommage dataset 2025), `qualifying`, `sprintRace` et `sprint` → `SPRINT` (renommage dataset 2025), `feature` → `RACE`. `_CIRCUIT_DATA` : 23 circuits. Module-level backward-compat functions conservées (importées directement par `test_f1calendar_source.py`).
- **F1CalendarSource (F3)** : config F3 uniquement — `_series_key="f3"`, sessions : `practice/qualifying/sprint/feature` (clés différentes de F2). `_CIRCUIT_DATA` : 13 circuits (subset F1 européen + Bahreïn + Melbourne). Pas de fonctions module-level (F3 n'a pas de tests hérités). URL construite par la base : `_BASE_URL/{series_key}/{year}.json`. `event_uid = f"f1calendar-{series_key}-{year}-{round}@motorsport-calendar"`.
- **F1CalendarSource (F1 Academy)** : config F1A uniquement — `_series_key="f1-academy"`, sessions : `fp1/fp2/qualifying1/qualifying2/race1/race2/race3`. `_CIRCUIT_DATA` : 15 circuits (2023-2025). Mapping contraint : `race2 → FP3` workaround ADR-016. Package Python : `f1_academy` (underscore), championship ID : `"f1-academy"` (tiret). `qualifying2` optionnel (absent en 2025+).
- **test_cli_generate.py — imports disambiguïsés** : F1Academy importée comme `F1AcademyCalendarSource`, mockée dans `test_all_providers_fail_*`.
- **F1CalendarBaseSource** : dans `providers/support_series/f1calendar_base.py`. `__init__(client, cache, *, refresh)`, `get_season(year)`, `fetch_json(url, params)`, `_resolve_circuit_data(slug)`, `_build_circuit(event_data)`, `_build_event(championship, event_data, year)`. `_build_session(timestamp, type, duration, title)` = fonction module-level pure.
- **`_build_circuit` met `Circuit.name = Event.name`** (Sprint 32 — investigation) : le dataset `sportstimes/f1` (F2/F3/F1 Academy) n'a pas de champ "nom de circuit" dédié, seulement un descriptif court de manche (`name`, ex. "Belgian") et un `location` (plus proche d'un nom de circuit que d'une ville, ex. "Spa-Francorchamps", mappé sur `Circuit.city`). Le code réutilise `name` pour `Circuit.name` aussi, d'où `Event.name == Circuit.name` pour ces 3 séries (jamais pour F1/Jolpica, qui a un vrai `circuitName`). Corrigible côté provider (utiliser `location` pour `Circuit.name`) mais non fait — compensé côté présentation par `gui/event_display.py` (ADR-023) sans toucher au provider.
- **Mock pour tests support series** : `patch.object(F1CalendarSource, "fetch_json", AsyncMock(return_value=...))` — fonctionne même si `fetch_json` est hérité de la base (patch sur la sous-classe). Pour F3 : `patch("motorsport_calendar.providers.formula3.sources.f1calendar.F1CalendarSource.fetch_json", ...)`. **La valeur de retour doit utiliser `"races"` comme clé, jamais `"events"` (ADR-017)**.
- **Dataset key `"races"`** : le dataset `sportstimes/f1` utilise `"races"` comme clé de premier niveau, **pas** `"events"`. Toutes les fixtures de test (réelles ou mock) doivent utiliser `{"races": [...]}`. `"events"` est silencieusement ignoré. Tests de régression dans `TestRacesKeyRegression` (`test_f1calendar_base.py`) et `tests/fixtures/real/`.
- **HttpCache mock** : depuis Sprint 18, patcher `"motorsport_calendar.providers.support_series.f1calendar_base.HttpCache"` (et non plus `f1calendar.HttpCache`).
- **test_cli_generate.py — imports disambiguïsés** : F2 importée comme `F2CalendarSource`, F3 comme `F3CalendarSource` pour éviter les conflits de noms dans les tests "tout échoue". Les deux sont mockées dans `test_all_providers_fail_*`.
- **Ajouter un support series** (F3, Academy, Supercup) : créer `providers/XYZ/` avec source héritant de `F1CalendarBaseSource` — 4 overrides, ~15 lignes. Même pattern que `F1CalendarSource`.
- **JolpicaSource** : endpoint `http://api.jolpi.ca/ergast/f1/{year}/races.json?limit=100`. Requête unique par saison. Sessions extraites des champs `FirstPractice`, `SecondPractice`, `ThirdPractice`, `Qualifying`, `SprintQualifying`, `Sprint` + champ race (`date`+`time` au top level). Durées inférées : FP 60 min, Qualifying 60 min, SprintQualifying 45 min, Sprint 35 min, Race 130 min. `circuitId` snake_case → IANA timezone. Fallback noon UTC si `time` absent (pré-2000). Enregistrée sous `"jolpica"` dans SourceRegistry.
- **ErgastSource** : alias backward-compat pour `JolpicaSource` (Ergast arrêté fin 2024). Non enregistrée dans SourceRegistry — utiliser `source: jolpica` dans config.yaml.
- **Architecture figée (Sprint 10)** : ProviderRegistry + SourceRegistry = architecture finale. Pas de refactoring structurel prévu.
- **ProviderRegistry** : singleton `motorsport_calendar.core.registry.registry`. Factory signature : `(source) → Provider`. `registry.discover()` importe chaque `providers/X/__init__.py`.
- **SourceRegistry** : singleton `motorsport_calendar.core.source_registry.source_registry`. Clé `(championship_id, source_name)`. Factory signature : `(cache, refresh) → Source`. `source_registry.discover()` importe chaque `providers/X/sources/__init__.py`.
- **Auto-enregistrement providers** : `providers/formula1/__init__.py` → `registry.register("formula1", _make_provider)`.
- **Auto-enregistrement sources** : `providers/formula1/sources/__init__.py` → `source_registry.register("formula1", "openf1", lambda cache, refresh: OpenF1Source(...))`.
- **CLI orchestration** : `source = source_registry.get(cid, source_name)(cache, refresh)` → `provider = registry.get(cid)(source)`.
- **ProviderConfig** : `enabled: bool = True` (opt-out), `source: str = ""` (optionnel — `or "openf1"` dans la CLI).
- **ProvidersConfig.get(id)** : cherche dans les champs nommés (`formula1`, `wec`) puis dans `model_extra`. Retourne `None` si absent (= activé par défaut dans `registry.enabled()`).
- **Ajouter une source** : une ligne dans `sources/__init__.py`. Aucun autre fichier.
- **Ajouter un championnat** : créer `providers/X/` + `providers/X/sources/`. Aucun autre fichier pour le
  backend (`registry.discover()`/`generate` CLI le voient automatiquement — vérifié en conditions réelles
  au Sprint 34 avec Formula E, y compris via `F1CalendarBaseSource` quand la série partage le dataset
  f1calendar). Pour la visibilité GUI complète (Wizard/"Ce week-end"), 3 lignes supplémentaires
  seulement : `categories.py` (groupe), `display_names.py` (nom lisible),
  `upcoming_weekend.py::WEEKEND_CHAMPIONSHIP_IDS` — zéro changement dans `main_view.py`/`calendar.py`
  (entièrement pilotés par la donnée). Ajouter aussi une commande CLI dédiée (`generate-X`) suit
  `cli.py::_run_generate_command()` depuis le Sprint 34 (voir ADR-025) — plus de copier-coller par
  commande.
- **Design system GUI (Sprint 26)** : toute couleur/spacing/radius/taille d'icône dans `gui/views/*` ou `gui/main_view.py` vient de `gui/theme.py` — jamais `ft.Colors.*` ni un entier brut. Palette écosystème = `theme.BAppsColors`, palette produit = `theme.MotorsportColors`, les vues n'importent que `theme.Colors` (rôles sémantiques).
- **Wizard "Mon calendrier"** : `GenerateState.current_step` (0-3) pilote quelle étape `views/calendar.py` rend. `state.can_advance()`/`can_go_back()` gèrent le gating — jamais de logique de validité dans `calendar.py` (layout pur, il ne fait que lire `c.current_step`).
- **Logo** : pas de SVG définitif dans le dépôt — `theme.logo_placeholder(kind, size)` partout où le logo apparaîtra. Voir `gui/assets/logo/README.md` pour la liste des emplacements et la procédure d'intégration future.
- **Flet 0.85 — couleurs `WHITE*` dépréciées** : utiliser `ft.Colors.WHITE_12/30/38/54/60/70` (underscore), pas `WHITE12` etc. — sinon `DeprecationWarning` à chaque construction du contrôle. Déjà corrigé dans `theme.py`.
- **`Event.championship.id` ≠ id du registre** (Sprint 29, ADR-020) : chaque provider suffixe par l'année (`"formula1-2026"`). Toute logique GUI qui doit regrouper/afficher par championnat (comme `categories.get_groups_for` ou `display_names.get_display_name`) doit recevoir l'id du registre séparément — jamais le déduire de `event.championship.id`. Pattern : `upcoming_weekend.WeekendEntry(championship_id, event)`.
- **Défaut de source par championnat** : `ConfigService().providers.get(cid)` peut avoir un défaut EXPLICITE différent du premier enregistré dans `source_registry` — ex. `formula1` défaut sur `"openf1"` (voir `config/models.py`), pas `"jolpica"` bien qu'enregistrée en premier. Toujours vérifier `ProvidersConfig` avant de mocker une source dans un test qui passe par `ConfigService` réel.
- **"Ce week-end" (Sprint 29)** : `gui/upcoming_weekend.py` (logique pure, testée sans HTTP) + `controller.get_upcoming_weekend()` (fetch, mirroir de `generate_calendar`). Périmètre figé à `upcoming_weekend.WEEKEND_CHAMPIONSHIP_IDS` (5 championnats), indépendant de l'opt-out `config.yaml`. Recherche vendredi-dimanche en **UTC** (limite documentée pour les circuits très à l'est) ; l'heure *affichée* par session est convertie dans `Circuit.timezone` (IANA, déjà présent dans le modèle). Fetch déclenché une fois au lancement de l'app (`main_view.py::_load_weekend`, référencé sur `page.weekend_load_task`), jamais à chaque visite d'onglet — repose sur le TTL de `HttpCache` (24h par défaut) pour l'aspect "jamais de réseau à chaque ouverture".
- **Bibliothèque de composants `gui/components/`** (Sprint 30, ADR-021) : distincte de `gui/views/` (une page = un module) et de `gui/theme.py` (tokens/primitives). Un composant définit son propre modèle de données (déjà mis en forme — jamais un `Event`/`Session`/`Championship`/`Circuit`), se construit uniquement à partir des primitives `theme.py`, et expose ses points d'extension explicitement (ex. `footer: ft.Control | None` sur `championship_card.build_championship_card` — le composant place le contrôle fourni sans jamais l'interpréter). Premier composant : `components/championship_card.py` (`ChampionshipCardData`, `SessionRow`, `build_championship_card`) — utilisé par `upcoming_weekend.py`/`views/weekend.py`, prévu pour Favoris/Recherche/Tableau de bord/Notifications/Historique. Deuxième composant : `components/championship_selector.py` (Sprint 44, extrait de `views/calendar.py` — Sprint 43 — une fois "Mes favoris" devenu un second consommateur réel) : `ChampionshipButtonData`/`ChampionshipCategoryData`/`build_championship_selector` — l'accordéon-de-boutons-sélectionnables partagé par "Mon calendrier" et "Mes favoris", chacun décidant ce que "sélectionné" signifie (génération vs favori) sans que le composant le sache. Prochain composant du même genre : suivre exactement ce patron (paquet `components/`, modèle dédié, primitives `theme.py` uniquement, point d'extension explicite plutôt qu'anticipé, extraction seulement au second consommateur réel).
- **Layout System `gui/components/layout/`** (Sprint 31, ADR-022) : `PageContainer`/`PageHeader`/`Section`/`SectionHeader`/`CardList`/`EmptyState`/`PageSpacing`. Nommage PascalCase délibéré (widget-style — exception `ruff.toml` `N802` scopée à ce paquet). Toute nouvelle vue doit se composer ainsi : `PageContainer(header=PageHeader(titre, icon=...), body=[Section(...)])` — jamais de `theme.page_shell`/`theme.section_title`/`theme.card` appelés directement dans une vue pour ces besoins (le Layout System les enveloppe déjà). Règle : `PageHeader` est **toujours séparé** du corps, jamais absorbé dans une carte de contenu (changement Sprint 28→31, voir ADR-022) — seule exception assumée : `about.py`, qui garde son bloc de marque compact sans `PageHeader` du tout. Sprint 43 : `PageHeader`/`PageContainer` gagnent deux slots optionnels — `trailing` (contrôle secondaire en haut à droite du titre, ex. le sélecteur de saison de "Mon calendrier") et `footer` (contenu fixe qui ne défile jamais avec le corps, ex. le bouton "Créer mon calendrier") — tous deux `None` par défaut, donc un no-op total pour les 5 autres pages. `calendar.py` (ex-wizard, devenu page unique au Sprint 43) utilise désormais `Section` comme toute autre vue — l'ancien flux séquentiel d'étapes qui le justifiait d'en être exempté n'existe plus.
- **Normalisation des métadonnées d'événement `gui/event_display.py`** (Sprint 32, ADR-023) : toute vue qui construit une `ChampionshipCardData` à partir d'un `Event` domaine DOIT passer par `normalize_event_display(championship_id, event)` — ne jamais lire `event.name`/`circuit.name`/`circuit.city`/`circuit.country` directement pour peupler la carte (risque de doublon "X / X" ou de "Unknown" affiché tel quel, voir investigation Sprint 32). `circuit_name`/`country` valent `None` quand la ligne doit être masquée ; `build_championship_card` (composant) se contente d'omettre une ligne `None`, aucune décision n'est prise dans le composant.
- **Registre des identités visuelles `gui/championship_assets.py`** (Sprint 33, ADR-024) : point d'entrée unique `get_championship_asset(championship_id) -> ChampionshipAsset(logo_src: str | None)`. Jamais de chemin de fichier codé dans une vue/composant, jamais de `if championship_id == ...` — `ChampionshipCard` interroge ce registre et rend un `ft.Text` nu si `logo_src is None` (id inconnu OU logo pas encore livré, indistinguables côté appelant), sinon `ft.Row([ft.Image(logo_src, IconSize.LG), ft.Text(...)])`. Aucun logo officiel livré dans le dépôt à ce stade (voir `gui/assets/championships/README.md`, même patron que `gui/assets/logo/README.md`) — le rendu actuel de toutes les cartes est donc strictement identique à avant ce sprint. Ajouter un logo demain = déposer le fichier au nom attendu dans `gui/assets/championships/`, zéro ligne de code à changer. Attention Flet 0.85.3 : l'enum s'appelle `ft.BoxFit`, pas `ft.ImageFit` (n'existe pas dans cette version).

---

## Dette technique

| Item | Impact | Priorité |
|---|---|---|
| `export` CLI est un stub (exit 1) | Commande inutilisable | HAUTE |
| `ErgastSource` → alias `JolpicaSource` | ✅ Résolu — Sprint 15 | — |
| `CachedFormula1Source` non implémentée | Appels répétés à l'API | HAUTE |
| Cache `.cache/` en CWD (pas `~/.cache/`) | ✅ Résolu — Sprint 49 (`utils/paths.py::user_cache_dir`, `$XDG_CACHE_HOME`/`%LOCALAPPDATA%`) | — |
| `core/service.py::CalendarService` jamais appelée par le CLI ni la GUI | Scaffolding orphelin (pas "vide" — contient du vrai code depuis l'origine, correction du terme) ; un bug réel y dormait (passait un `Championship` à `Exporter.export()` au lieu d'événements), **corrigé Sprint 50** (audit mypy complet), toujours non câblée nulle part | BASSE — corrigée mais reste sans appelant réel ; utile seulement si une future API programmatique en a besoin |
| `utils/logging.py` vide | Pas de logs structurés | BASSE |
| Couverture `cli.py` à 90 % (branches KeyError source/provider inconnue, cas d'erreur générique de `generate`) | Branches non testées, réduites au Sprint 34 (factorisation `_run_generate_command`, 355→182 lignes) | BASSE |
| mypy `main_view.py`/`championship_selector.py`/`about.py`/`views/dashboard.py`/`views/support.py` : signatures `on_click`/`on_change`/`on_select` Flet 0.80 (code) vs 0.85.3 (installé) | 41 erreurs mypy au total (préexistant, non bloquant — tests runtime passent), même famille depuis Sprint 26 ; auditée en détail Sprint 50 (`docs/AUDIT.md` §4) ; Sprint 51 ajoute 3 occurrences (`update_check_task`, boutons de la boîte de dialogue "nouvelle version") ; Sprint 52 ajoute 12 occurrences de plus du même schéma toléré (les 2 `ft.Switch`/3 `ft.Dropdown` de la page Préférences × leurs call sites) ; Sprint 53 ajoute 1 occurrence de plus (le nouveau `ft.Button(on_click=on_view_release)` de la carte "Nouveautés" du Dashboard) ; Sprint 57 ajoute 2 occurrences de plus (les boutons Discussions/Issues de "Soutenir le projet"), pas une nouvelle catégorie — non corrigée car réécrire les signatures de callback risquerait une régression comportementale pour un gain de vérification statique seulement ; à réévaluer si une version de Flet aligne enfin ses stubs sur le runtime | MOYENNE |
| `motorsport_calendar/gui/preferences.py` : `dict` sans paramètres de type (`type-arg`) | ✅ Résolu — Sprint 50 (`dict[str, Any]`, audit mypy complet) | — |
| Logo Motorsport Calendar toujours en placeholder (`theme.logo_placeholder()`) | Rendu visuel non définitif | BASSE — bloqué sur livraison des SVG Brand Set v1.0 |
| Découpage vendredi-dimanche de "Ce week-end" ancré UTC, pas sur le fuseau du circuit | Un week-end très à l'est (Japon, Singapour…) peut être mal détecté à la marge | BASSE — cas limite documenté, heure affichée déjà correcte |
| `f1calendar_base.py::_build_circuit` réutilise `event.name` pour `Circuit.name` (F2/F3/F1 Academy) | Cause du doublon "Belgian/Belgian" — compensé côté présentation (`event_display.py`) mais pas corrigé à la source | MOYENNE — voir ADR-023, corrigible en utilisant `location` pour `Circuit.name` |
| Tables `_CIRCUIT_DATA` (pays) incomplètes pour F2/F1 Academy | Ligne pays masquée dans "Ce week-end" au lieu d'afficher un vrai pays | BASSE — comportement correct (jamais "Unknown"), juste incomplet |
| Aucun logo officiel de championnat livré (`gui/assets/championships/`) | `ChampionshipCard` ne montre jamais de logo en pratique — registre (Sprint 33, ADR-024) architecturalement prêt mais invisible | BASSE — bloqué sur livraison des fichiers + décommentage `assets_dir=` dans `gui/app.py` |
| `OfficialImsaSource` non implémentée | "Ce week-end" n'affichera jamais de carte IMSA en pratique | HAUTE — bloque aussi `generate`/`generate-imsa` ; aucune source stable trouvée au Sprint 36, ré-investigué et reconfirmé au Sprint 48 (imsa.com bloque désormais jusqu'à son propre `robots.txt`), voir `docs/DATA_SOURCES.md` et ADR-027 |
| `formula-e`/`elms`/`mlmc` absents de `championship_assets.py::_LOGO_FILENAMES` | Contrairement aux 5 championnats les plus anciens, aucun nom de fichier logo n'est prévu pour ces 3 championnats | BASSE — volontaire, Sprints 34-35 "aucun travail sur les icônes" ; ajouter une ligne par championnat le jour où un logo est prévu |
| `AcoSportsEventSource.get_season(year)` ne fonctionne que pour la saison courante | `europeanlemansseries.com`/`lemanscup.com` ne publient pas d'archive par année à une URL prévisible | BASSE — limitation du site source, propage `HTTPStatusError` proprement, cohérent avec le reste du projet |
| `SroTimetableSource.get_season(year)` ne fonctionne vraisemblablement que pour la saison publiée courante (non explicitement testé sur une année passée) | Même limitation probable qu'ACO — non confirmée au Sprint 37 (hors périmètre demandé) | BASSE — à vérifier si un besoin d'archive apparaît |
| Durée de course "N Hour(s)" inférée uniquement depuis le slug d'URL ; formats sans motif horaire explicite (ex. `suzuka-1000km`) utilisent une durée par défaut approximative | Durée de session légèrement inexacte pour ces formats précis | BASSE — comportement documenté, cohérent avec les durées par défaut déjà utilisées par `JolpicaSource`/`OpenF1Source` |
| `gtwc-europe`/`gtwc-america`/`gtwc-asia`/`igtc` absents de `championship_assets.py::_LOGO_FILENAMES` | Aucun logo prévu pour ces 4 championnats | BASSE — volontaire, Sprint 37 "aucun travail sur les icônes" ; ajouter une ligne par championnat le jour où un logo est prévu |
| `OfficialWorldSbkSource` non implémentée | "Ce week-end" n'affichera jamais de carte WorldSBK en pratique | HAUTE — bloque aussi `generate`/`generate-worldsbk` ; aucune source stable trouvée au Sprint 38, ré-investigué au Sprint 48 (nouveaux hôtes Pulselive découverts — `api.wsbk.pulselive.com`/`wsbk.pulselive.com` — aucun n'expose d'endpoint événements exploitable), voir `docs/DATA_SOURCES.md` et ADR-029 |
| Durée de course WEC déduite du nom de l'épreuve, pas d'un signal du JSON-LD | Un nom de course futur ne correspondant ni au motif "X Hours" ni à `_NAMED_RACE_DURATION_HOURS` se rabat sur 6h par défaut (correct pour la saison 2026, à surveiller ensuite) | BASSE — `OfficialWecSource._race_session_end`, voir ADR-039 ; documenté, pas un bug |
| `WEC_CIRCUIT_DATA`/`WEC_ADDRESS_COUNTRY_CODES` incomplètes pour les circuits hors calendrier 2026 (ex. Silverstone, déjà au calendrier 2027) | Circuit affiché avec un fuseau UTC/pays générique tant que la table n'est pas complétée | BASSE — tables "non exhaustives" par convention (même principe que toutes les autres tables `_CIRCUIT_DATA` du projet), à compléter au fil des saisons |
| Durée RACE/SPRINT MotoGP/Moto2/Moto3 approximative (source ne fournit jamais d'heure de fin réelle pour ces sessions) | Durée de session légèrement inexacte pour Race/Sprint uniquement (FP/Qualifying ont des heures réelles) | BASSE — comportement documenté, mêmes valeurs par défaut par classe/format que le reste du projet |
| `motogp`/`moto2`/`moto3`/`worldsbk` absents de `championship_assets.py::_LOGO_FILENAMES` | Aucun logo prévu pour ces 4 championnats | BASSE — volontaire, Sprint 38 "aucun travail sur les icônes" ; ajouter une ligne par championnat le jour où un logo est prévu |
| Dashboard non vérifié visuellement (Flet réel) | Structure des contrôles vérifiée, rendu pixel non confirmé — Sprint 53 y ajoute 3 sections ("Nouveautés"/"Accès rapides"/"État"), toujours non rendues | BASSE — même limitation que chaque sprint GUI précédent, bloqué sur un poste avec affichage ; voir piste `-17` ci-dessus |
| Résumé de sélection "Mon calendrier" non vérifié visuellement (Flet réel) | Structure des contrôles + 25 tests vérifiés, rendu pixel non confirmé | BASSE — même limitation que chaque sprint GUI précédent, bloqué sur un poste avec affichage |
| Explorateur de saison "Mon calendrier" non vérifié visuellement (Flet réel) | Structure des contrôles + 23 tests vérifiés, rendu pixel non confirmé — attention particulière requise sur une longue liste (17 championnats sélectionnés) | BASSE — même limitation que chaque sprint GUI précédent, bloqué sur un poste avec affichage |
| Fiche événement (boîte de dialogue) non vérifiée visuellement (Flet réel) | Structure des contrôles + 20 tests vérifiés, rendu pixel non confirmé — attention particulière requise sur un événement à beaucoup de sessions (scroll interne de la boîte de dialogue) | BASSE — même limitation que chaque sprint GUI précédent, bloqué sur un poste avec affichage |
| Refonte "Mon calendrier" (Sprint 43) non vérifiée visuellement (Flet réel) | Structure des contrôles + ~65 tests vérifiés, aucun rendu pixel confirmé — sprint le plus visuellement significatif à ce jour (accordéons, boutons sélectionnables, pied de page fixe) | MOYENNE — plus haut risque que les sprints GUI précédents vu l'ampleur du changement de mise en page ; bloqué sur un poste avec affichage |
| "Mes favoris" (Sprint 44) non vérifiée visuellement (Flet réel) | Structure des contrôles + 36 tests vérifiés (pipeline Dashboard/Ce week-end/Mon calendrier revalidé en direct sans mock), aucun rendu pixel confirmé | BASSE — réutilise l'accordéon déjà en place pour "Mon calendrier", même limitation que chaque sprint GUI précédent |
| ✅ Résolu — Sprint 52 : `PreferencesModel.favorite_championships` retiré (jamais branché) | La page Préférences est désormais réelle et affiche le nombre de favoris via `FavoritesService.list()` directement — le champ décoratif n'existe plus, `PreferencesModel` ne porte plus que les 3 champs "Application" (théme/langue/format horaire) | — |
| "Recherche" (Sprint 45) non vérifiée visuellement (Flet réel) | Structure des contrôles + 46 tests vérifiés (dont un smoke test en direct sans mock contre chaque exemple explicite du brief), aucun rendu pixel confirmé | BASSE — même limitation que chaque sprint GUI précédent, bloqué sur un poste avec affichage |
| Recherche limitée à l'année actuellement parcourue sur "Mon calendrier" (`year_events`) | Un événement/circuit d'une autre année n'apparaît pas tant que "Mon calendrier" n'a pas chargé cette année | BASSE — volontaire, cohérent avec "aucun appel réseau supplémentaire" (brief Sprint 45) ; documenté, pas un bug |
| ✅ Résolu — Sprint 52 : `NotificationService` câblé (page Préférences) | Les 3 réglages (activé, favoris uniquement, délai) sont désormais pilotables depuis l'interface — reste néanmoins sans notification système réelle (Windows/Linux/macOS), toujours hors périmètre | BASSE — aucune notification OS envoyée à ce jour, seul le moteur de calcul + ses 3 préférences sont exposés |
| `kinds`/plusieurs `lead_times` non persistés comme préférences (`NotificationService`) | Seuls `notifications_enabled`/`notifications_default_lead_time_minutes`/`notifications_favorites_only` sont persistés, exactement les 3 préférences demandées par le brief Sprint 46 | BASSE — volontaire, `kinds`/plusieurs délais simultanés restent des paramètres d'appel, pas encore une option utilisateur |
| Aucune page "Circuits" dédiée (`CircuitService`, Sprint 47) | `list_circuits()` existe et est testé mais n'est exposé nulle part dans la navigation — la fiche Circuit ne s'ouvre que depuis un clic dans la fiche événement | BASSE — volontaire, non demandé par le brief Sprint 47 ; voir piste `-11` ci-dessus |
| Historique des événements de la fiche Circuit non cliquable (`CircuitEventEntry`) | `championship_id`/`event_uid` sont déjà portés par chaque entrée mais jamais interprétés — cliquer une ligne de l'historique n'ouvre pas la fiche événement correspondante | BASSE — volontaire, non demandé par le brief Sprint 47 ; identité déjà présente pour un futur sprint, voir piste `-10bis` ci-dessus |
| Build Windows (`flet build windows`) jamais exécuté | Procédure/prérequis transcrits depuis la documentation officielle Flet (`docs/PACKAGING.md` §3) mais jamais vérifiés en conditions réelles | MOYENNE — aucune machine Windows disponible dans cet environnement ; Flet ne permet pas la cross-compilation depuis Linux/macOS, voir piste `-13bis` ci-dessus |
| ✅ Résolu — Sprint 59 : le build Linux plantait au démarrage (`ModuleNotFoundError`, identifié Sprint 58) | Corrigé par `motorsport_calendar/gui/pyproject.toml` (`tool.flet.dev_packages`) — vérifié par un rebuild réel + 2 lancements du binaire, aucune trace d'erreur | — |
| ✅ Résolu — Sprint 59 : identité de l'application non configurée dans le build (Sprint 58) | Exécutable/ID d'application/titre de fenêtre corrigés (même manifeste que la ligne ci-dessus) — `motorsport-calendar`/`com.flet.motorsport-calendar` | — |
| Version embarquée dans le build encore `1.0.0` au lieu de `0.2.0` (Sprint 59, non résolu) | `project.version = "0.2.0"` déclaré dans le manifeste de build mais `data/flutter_assets/version.json` continue de rapporter `1.0.0` — cause exacte non retracée | BASSE — cosmétique, n'affecte ni le démarrage ni le comportement de l'app ; voir piste `-23` ci-dessus |
| Aucun fichier `.desktop` Linux (Sprint 58) | Le build ne s'intègre pas au menu Applications — l'utilisateur doit lancer l'exécutable directement | BASSE — attendu pour un `.deb`/AppImage/Flatpak futur, pas bloquant pour une distribution en archive ; voir piste `-21ter` ci-dessus |
| `theme.logo_placeholder()` non câblé sur les vrais assets Brand Set v1.0 (Sprint 49) | Les fichiers officiels (`gui/assets/logo/*.svg`) sont livrés et bundlés dans le build mais aucune vue ne les consomme encore | BASSE — volontaire, hors périmètre "aucun changement de Design System" du brief Sprint 49 ; voir piste `-13ter` ci-dessus |
| `gui/main_view.py::build_main_view` — fonction de 771 lignes (89 % du fichier) | Identifiée par l'audit Sprint 50 comme seule vraie anomalie de taille du projet — voir `docs/AUDIT.md` §6 | MOYENNE — non découpée ce sprint (risque de régression comportementale pour un gain de lisibilité), voir piste `-14` ci-dessus |
| `gui/theme.py::BAppsColors` jamais consommée par aucun token sémantique | Classe de documentation-as-code (traçabilité de la provenance du Brand Set) confirmée intentionnelle par l'audit Sprint 50, pas un oubli | BASSE — signalée dans `docs/AUDIT.md` §3, non supprimée |
| mypy `tests/` : 176 erreurs restantes après relâchement Sprint 50 | Écho de la même famille Flet stub-version dans les tests qui construisent/inspectent des contrôles, plus le bruit résiduel normal du typage dynamique `unittest.mock` (aucune vraie erreur de logique de test derrière) ; Sprint 57 ajoute 19 occurrences de plus du même bruit (`test_gui_url_opener.py` teste `make_url_opener` avec un double factice plutôt qu'un vrai `ft.UrlLauncher`/`ft.ControlEvent`, `TestComingSoonRow` accède à `.content`/`.icon` sur un `ft.Control` non affiné), pas une nouvelle catégorie | BASSE — détaillé dans `docs/AUDIT.md` §4, non traité au cas par cas (des dizaines de sites, aucun ne révèle un vrai bug) |
| `config.update.manifest_url` vide par défaut (Sprint 51) | La vérification de mise à jour est fonctionnelle et testée mais un no-op silencieux tant qu'aucun manifeste réel n'est publié et référencé dans `config.yaml` | BASSE — volontaire, "ne pas coupler la logique à GitHub" (brief) ; voir piste `-15` ci-dessus |
| ✅ Résolu — Sprint 52 : `update_check_enabled` a désormais une UI (page Préférences) | — | — |
| `PreferencesModel.theme`/`language`/`time_format` non implémentés (Sprint 52) | Section "Application" de la page Préférences reste "Disponible prochainement" — typage prêt, aucune persistance ni lecture ailleurs dans l'app | BASSE — volontaire, brief explicite "préparer, sans forcément implémenter" ; voir piste `-15bis` ci-dessus |
| Page Préférences (Sprint 52) non vérifiée visuellement | Structure des contrôles + 9 tests nets vérifiés, aucun rendu pixel confirmé — premier usage de `ft.Switch`/`ft.Dropdown` multiple dans ce projet | BASSE — même limitation que chaque sprint GUI précédent, poste avec affichage indisponible ; voir piste `-16` ci-dessus |
| Nouvelles sections du Dashboard (Sprint 53) non vérifiées visuellement | Structure des contrôles + 29 tests nets vérifiés, aucun rendu pixel confirmé — 3 nouvelles sections ("Nouveautés"/"Accès rapides"/"État") | BASSE — même limitation que chaque sprint GUI précédent, poste avec affichage indisponible ; voir piste `-17` ci-dessus |
| `config.update.manifest_url` vide par défaut (Sprint 51/53) | La carte "Nouveautés" du Dashboard (Sprint 53) et la boîte de dialogue de démarrage (Sprint 51) sont toutes deux fonctionnelles et testées mais restent un no-op silencieux tant qu'aucun manifeste réel n'est publié | BASSE — volontaire, "ne pas coupler la logique à GitHub" (brief Sprint 51) ; voir piste `-17bis` ci-dessus |
| ✅ Résolu — Sprint 54 : icônes d'en-tête "Mes favoris"/Dashboard incohérentes | Alignées sur la variante pleine du rail de navigation, comme chaque autre page | — |
| ✅ Résolu — Sprint 54 : emoji "✅" codé en dur dans la boîte de dialogue de succès | Remplacé par un `ft.Icon(CHECK_CIRCLE)` thématisé, cohérent avec le reste de l'app (icônes Material uniquement, jamais d'emoji) | — |
| ✅ Résolu — Sprint 54 : 8 espacements codés en dur (`spacing=2`/`spacing=4`) hors du Design System | Remplacés par `theme.Spacing.XXS` dans `views/preferences.py`/`about.py`/`search.py`/`calendar.py`/`main_view.py` | — |
| ✅ Résolu — Sprint 54 : ponctuation incohérente des titres `EmptyState` | Standardisée — titres courts sans point, phrases instructives avec verbe gardent le leur | — |
| ✅ Résolu — Sprint 54 : "À propos" affichait "Version Alpha" sans numéro | Affiche désormais `motorsport_calendar.__version__`, cohérent avec le Dashboard (Sprint 53) | — |
| ✅ Résolu — Sprint 54 : `nav_home`/`nav_calendar` chaînes mortes | Supprimées — jamais référencées depuis l'introduction de `nav_dashboard`/`nav_my_calendar` | — |
| ✅ Résolu — Sprint 55 : résultats de "Recherche" non cliquables | Chaque résultat ouvre désormais la vue existante correspondante (Mon calendrier/fiche événement/fiche Circuit) | — |
| Recherche interactive (Sprint 55) non vérifiée visuellement | Structure des contrôles + 10 tests nets vérifiés, aucun clic/rendu pixel confirmé | BASSE — même limitation que chaque sprint GUI précédent, poste avec affichage indisponible ; voir piste `-19` ci-dessus |
| Aucune notification système réellement affichée (Sprint 56) | `NullSystemNotifier` est la seule implémentation — l'abstraction est prête et testée mais aucune notification n'est jamais visible en usage réel | HAUTE en apparence, en réalité BASSE — Flet ne fournit aucune solution native à ce jour (fait vérifié, pas une hypothèse, voir entrée #56) ; volontaire, "ne pas bricoler" (brief) ; voir piste `-20` ci-dessus |
| IMSA/WorldSBK masqués des sélecteurs GUI (Sprint 57) | `controller.list_championships()` les exclut tant qu'aucune source fiable n'existe — volontaire, positionnement produit avant la Beta | BASSE — les deux restent enregistrés dans `ProviderRegistry` (`registry.list_all()` intégral), aucune suppression de code ; voir piste `-20bis` ci-dessus |
| PayPal/GitHub Sponsors sans lien réel (Sprint 57) | "Soutenir le projet" affiche 2 `ComingSoonRow` — la structure est prête, aucune vraie URL n'est configurée | BASSE — volontaire, "aucun lien réel n'est encore nécessaire" (brief) ; voir piste `-21` ci-dessus |
| "À propos"/"Soutenir le projet" (Sprint 57) non vérifiées visuellement | Structure des contrôles + 19 tests nets vérifiés (4 About, 15 Support), aucun rendu pixel confirmé | BASSE — même limitation que chaque sprint GUI précédent, poste avec affichage indisponible |

---

## Commandes utiles

```bash
# Lancer les tests
python -m pytest

# Lancer les tests du cache uniquement
python -m pytest tests/test_http_cache.py -v

# Générer un calendrier F1 2024 (utilise le cache)
motocal generate-f1 2024 f1-2024.ics

# Forcer un re-téléchargement (ignore le cache)
motocal generate-f1 2024 f1-2024.ics --refresh
```
